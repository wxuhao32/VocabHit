# VocabHit Review 系统深度分析报告

> 分析日期：2026-08-17  
> 基于代码版本：VocabHit 0.7.13  
> 涉及文件：`js/app.js`（第412-812行）、`css/style.css`（第930-1100行）、`index.html`（第287-301行）

---

## 一、记忆效果：主动回忆（Active Recall）机制分析

### 1.1 当前流程的主动回忆强度评估

**当前流程（代码追踪）：**

```
renderReviewCard()          ← 显示单词 + 自动TTS播放2次
    ↓ 用户点击 认识/模糊/不认识
onReviewAnswer()            ← 立即记录到记忆曲线
    ↓
renderReviewDetail()        ← 显示释义、音标、历史统计
    ↓ 用户点击"下一步"
goSpell() → renderReviewSpell()  ← 拼写环节（中文释义提示）
    ↓
reviewNext()               ← 下一个单词
```

#### ✅ 已经做到的部分

| 机制 | 实现位置 | 效果 |
|------|---------|------|
| 先显示词形，后显示释义 | `renderReviewCard()` vs `renderReviewDetail()` | 基础主动回忆 ✓ |
| 三档判断 | `known/fuzzy/unknown` | 区分记忆强度 ✓ |
| 艾宾浩斯间隔 | `R_INTERVALS = [0,3,7,10,20,30,30]` | 科学间隔 ✓ |
| 会话持久化中断恢复 | `REVIEW_SESSION_KEY` | 不丢失进度 ✓ |
| "记错了"回滚机制 | `reviewCorrection()` | 元认知修正 ✓ |

#### ⚠️ 关键问题：主动回忆被削弱的因素

##### 问题 A：判断页自动播放 TTS —— 答案泄露风险

```javascript
// app.js 第662行
ttsSpeak(word, 2); // Review 判断页自动播放读音 2 次
```

**问题本质：** 对于听觉型学习者或短词（3-5字母），听到发音可能直接暴露词义，用户还没来得及从视觉上主动回忆就被声音"告诉"了答案。

**认知心理学依据：**
- **编码特异性原理（Encoding Specificity Principle）**：提取线索应与编码时一致。如果学习时是"看词→想义"，复习时也应该是"看词→想义"，而不是"听音→认词"。
- **测试效应（Testing Effect）**的核心是"提取努力（retrieval effort）"。TTS降低了提取难度，削弱了测试效应。

**建议方案：**
1. 判断页默认**不自动播放 TTS**，改为用户提供扬声器按钮手动点击
2. 仅在以下场景自动播放：
   - 显示释义后（detail 页）—— 此时已不是主动回忆阶段，而是确认/强化阶段
   - 用户点击"不认识"后 —— 帮助建立音义联系
3. 或者提供一个设置项："复习时自动朗读"，让用户自选

---

##### 问题 B：没有强制回忆延迟 —— "秒点"现象

当前代码中，`renderReviewCard()` 渲染后没有任何时间锁或延迟机制：

```javascript
// app.js 第646-663行 — 渲染后立即等待点击，无任何延迟
function renderReviewCard(body) {
  body.innerHTML = `...`;  // 单词 + 按钮
  ttsSpeak(word, 2);       // 立即播放
  // 没有 setTimeout、没有最小停留时间、没有任何"思考"引导
}
```

**实际问题：** 用户可以（并且很可能）在看到单词的 0.5 秒内就点击"认识"，根本没有进行真正的回忆操作。这把 Review 变成了**"快速点击游戏"而非记忆提取练习"**。

**认知心理学依据：**
- **加工深度理论（Levels of Processing）**：浅层加工（机械识别）vs 深层加工（语义提取）。当前设计鼓励浅层加工。
- **熟悉感错觉（Illusion of Competence）**：用户看到单词觉得"眼熟"就点认识，但这只是再认（recognition），不是回忆（recall）。再认的记忆保持效果远低于回忆。

**建议方案：**

**方案一：思考计时器（推荐）**
```
单词出现 → 显示 3 秒倒计时（静默，不焦虑）
         → 倒计时结束后才亮起三个按钮
         → 如果用户在这 3 秒内心里想了答案，按钮亮起时他的判断更准确
```

实现要点：
- 倒计时用微妙的动画（如底部细线渐进填充），不用数字倒计时（避免焦虑）
- 允许提前点击（如果用户已经想好了），但给一个视觉暗示"你可以再想想"
- 首次复习的新词可以用更长延迟（5秒），老词用较短延迟（2秒）

**方案二：主动回忆输入框**
```
单词出现 → 提示"在心中想它的意思"
         → 显示一个可选的"我想好了"按钮
         → 点击后才显示认识/模糊/不认识
```

这个方案的优点是把"我准备好了"变成显式动作，强迫用户至少有一次内部检索过程。

---

##### 问题 C：释义页信息过载 —— 干扰本次判断的真实性

```javascript
// app.js 第686-703行 — renderReviewDetail()
body.innerHTML = `
  <p class="review-detail-word">${wordHTML(word, m.total)}</p>  <!-- 单词+查询次数 -->
  <p class="review-detail-ph">${phoneticOf(word)</p>              <!-- 音标 -->
  ${senseLines(word).map(l => `<p>${l}</p>`).join('')}          <!-- 全部释义 -->
  <div class="review-stats">                                    <!-- 历史统计 -->
    <span><b>${st.known}</b> 认识</span>
    <span><b>${st.fuzzy}</b> 模糊</span>
    <span><b>${st.unknown}</b> 不认识</span>
  </div>
  <button id="review-wrong">记错了</button>
  <button id="review-next">下一步</button>
`;
```

**问题清单：**

| 信息 | 是否干扰主动回忆？ | 理由 |
|------|------------------|------|
| 单词上的查询次数上标 (`<sup>`) | ⚠️ 轻微 | 无关信息，增加视觉噪音 |
| 音标 | ❌ 不干扰 | 释义阶段的合理补充 |
| 全部释义行 | ❌ 不干扰 | 这是答案本身，应该显示 |
| **认识/模糊/不认识累计次数** | ⚠️⚠️ **严重** | **锚定效应！** 用户会受历史数据影响本次判断 |
| "记错了"按钮 | ⚠️ 轻微 | 功能需要，但增加了选择负担 |

**最严重的问题：历史统计数据造成的锚定效应（Anchoring Effect）**

当用户看到：
```
5 认识    2 模糊    1 不认识
```

他会潜意识认为："这个词我之前大部分时候都认识，这次也应该认识"。这完全破坏了每次复习的独立性。

**认知心理学依据：**
- **序列位置效应（Serial Position Effect）**：用户倾向于与上次判断保持一致。
- **确认偏误（Confirmation Bias）**：历史数据会强化用户的预设判断。

**建议方案：**
1. **判断阶段（answer page）：只显示单词和进度，其他一切都不显示** ✅ 强烈推荐
2. **释义阶段（detail page）：显示释义和音标，但不显示历史统计**
3. **完成页面（done page）：汇总展示本次所有统计数据** ← 统计移到这里

---

### 1.2 "认识/模糊/不认识"三档判断的有效性分析

#### 当前状态机逻辑（`answerReview()` 函数，第533-579行）：

```
认识(known):
  daily/booster → 恢复原长期阶段
  stage=0 → stage=1 (首次→3天)
  stage<6 → stage+=1 (推进)
  nextAt = businessDayAt(R_INTERVALS[stage])

模糊(fuzzy):
  有长期阶段 → booster模式，2天后
  无长期阶段 → daily模式，首次2天/之后1天

不认识(unknown):
  保留原长期阶段(如有)
  mode = daily，次日复习
```

#### ✅ 设计合理的部分

1. **模糊的"短期强化"机制**：`booster` 模式保留原阶段，2天后强化，这是一个很好的设计——既不惩罚太重，也不放过模糊记忆。
2. **不认识的"降级保护"**：不直接降 stage，而是进入 `daily` 循环，认识后恢复原阶段。这避免了偶发失误造成过度惩罚。
3. **新词首日不复习**：`nextDay4am()` 机制让新词有至少一天的沉淀期。

#### ⚠️ 可改进的部分

**问题：三档可能不够区分"假性认识"**

当前三档无法区分：
- **A 类认识**：看到单词，立即想起意思（< 1秒）→ 真正掌握
- **B 类认识**：看到单词，想了 2-3 秒才想起来 → 边缘记忆，容易遗忘
- **C 类模糊**：有印象但不确定具体意思 → 需要尽快复习

B 类和 C 类在当前系统中都被归入不同的档（B→认识，C→模糊），但实际上 B 类的记忆稳定性远不如 A 类。

**建议方案（见第五节详细设计）：引入"反应时间"作为辅助信号**

不需要改变用户界面（仍然三按钮），但在后台记录从单词显示到用户点击的时间差：
- < 2 秒点击"认识" → A 类（强记忆）
- 2-6 秒点击"认识" → B 类（弱记忆，下次间隔缩短）
- > 6 秒或点击"模糊" → C 类（模糊记忆）

这样可以在不增加用户认知负担的前提下，更精细地调整间隔。

---

### 1.3 拼写环节的记忆价值分析

#### 当前拼写机制（`renderReviewSpell()`，第732-760行）：

```javascript
// 拼写界面：中文释义提示 + 输入框 + 提交；结果不影响记忆曲线
const defHint = (senseLines(word)[0] || briefOf(word)) || "根据释义拼写单词";
body.innerHTML = `
  <p class="spell-hint">根据释义拼写单词</p>
  <p class="spell-def">${defHint}</p>          <!-- 中文释义提示 -->
  <input id="spell-input" placeholder="输入英文单词" />
  <button id="spell-submit">确认</button>
  <button id="spell-next">下一个</button>
`;
```

**关键设计决策：拼写错误不影响记忆曲线（注释明确说明）**

#### ✅ 拼写的正面价值

1. **产出式回忆（Production Recall）**：拼写是从语义到拼写的逆向提取，比再认（recognition）更深层的加工。
2. **多编码通道**：同时调动语义记忆、拼写模式记忆、运动记忆（打字）。
3. **错误检测功能**：拼写错误可以揭示"假性认识"——用户觉得自己认识，但拼不出来。

#### ⚠️ 当前设计的核心问题

**问题 A：拼写总是出现，无论记忆状态**

每个单词，无论用户选的是"认识"、"模糊"还是"不认识"，都会进入拼写环节。这导致：
- 已完全掌握的词（连续 5 次"认识"）仍需拼写 → 浪费时间，降低效率
- 刚标记"不认识"的词也要拼写 → 挫败感强，且此时拼写更像"抄写"而非"回忆"

**问题 B：拼写结果不影响记忆曲线**

```javascript
// app.js 第768行注释明确说明
/** 提交拼写：正确 → 绿 + 下一个；错误 → 红 + 正确拼写，须重新拼写（不改记忆曲线） */
```

这个设计的矛盾在于：
- 如果拼写真的有价值（如上所述），那它应该影响记忆判断
- 如果拼写只是附加强化，那它不应该强制出现

**问题 C：拼写提供了中文释义提示 —— 这降低了难度**

拼写页面显示中文释义（`spell-def`），然后要求拼写英文。这是"看中文写英文"，对于刚看过释义的用户来说，这更像是短期工作记忆的复述，而不是从长期记忆中提取。

**建议方案（详见第六节）：条件化拼写 + 结果反馈到记忆曲线**

核心思路：
1. **仅对"认识"的词触发拼写**（检验是否真认识）
2. **拼写错误 → 自动将本次判断降级为"模糊"**
3. **对"不认识"的词跳过拼写**（先记住意思再说）
4. **拼写时不提供中文释义提示**（或者提供但延迟 3 秒显示）

---

### 1.4 复习顺序与机械记忆风险

#### 当前顺序逻辑（`reviewQueue()`，第524-529行）：

```javascript
function reviewQueue() {
  return Object.keys(reviewStore.words)
    .filter((w) => isStarred(w) && reviewStore.words[w].nextAt <= now)
    .sort((a, b) => reviewStore.words[a].nextAt - reviewStore.words[b].nextAt);
}
```

**按 nextAt 升序排列** = 最 overdue 的词最先复习。

#### ⚠️ 潜在问题

1. **同一批词总是以相同顺序出现**：如果今天有 10 个词到期，它们每次都会按固定顺序出现。这可能导致**顺序依赖记忆**（用户记住了"下一个是 xxx"而不是真正认识了这个词）。

2. **没有随机化**：纯确定性顺序可能让用户形成"复习节奏预期"，减少每次回忆的努力度。

**建议方案：**
- 引入适度随机化：将队列分为小块（如 5 个一块），块内随机排序
- 或使用"伪随机"：优先级权重 = overdue 天数 * 0.7 + random() * 0.3

---

## 二、沉浸感：视觉与交互流程分析

### 2.1 当前视觉层级评估

#### Review 页面结构（index.html 第287-301行）：

```html
<main class="page" id="page-review">
  <header class="page-header">
    <button class="back-btn">← 返回</button>
    <h1 class="page-title">Review</h1>
    <button class="page-header-action" id="review-clear">清空</button>
  </header>
  <div class="review-body" id="review-body">
    <!-- 动态内容 -->
  </div>
</main>
```

#### CSS 视觉参数提取：

| 元素 | 参数值 | 评价 |
|------|--------|------|
| 页面背景 | `var(--bg)` = `#F7F7F8` | 与首页相同，无差异化 |
| 单词字号 | `38px` / `font-weight: 700` | ✅ 足够突出 |
| 进度文字 | `12.5px` / `color: var(--text-3)` | ✅ 低调，不抢焦点 |
| 按钮高度 | `52px` | ✅ 触控友好 |
| 卡片内边距 | `40px 22px 26px` | ✅ 留白充足 |
| Header | 标准页面 header（返回+标题+清空） | ⚠️ 与普通页面无差异 |

### 2.2 沉浸感不足的具体表现

#### 问题 A：Review 页面看起来像"普通列表页"

当前 Review 页面的 header 和容器样式与其他页面（生词、任务、坚持看板）完全一样。用户进入 Review 后没有一个明显的"环境切换"感知。

**对比参考（优秀沉浸感设计）：**
- **Anki**：复习卡片全屏，无其他 UI 元素
- **Duolingo**：进入练习后隐藏导航栏，顶部只有进度条和退出按钮
- **Quizlet**：学习模式使用深色背景+大字体，与浏览模式明显区分

**建议方案：Review 进入全屏/沉浸模式**

具体改动：
1. **进入 Review 时隐藏底部 TabBar**（通过添加 CSS class 实现）
2. **Header 简化为：左侧退出按钮 + 中间进度 +右侧暂停/设置图标**
3. **移除"清空"按钮**（放到设置里，避免误触）
4. **考虑使用独立的背景色**（如深色主题或柔和的渐变）

---

#### 问题 B：单词切换无过渡动画

当前代码中，`reviewNext()` 直接调用 `renderReviewByStep()`，没有任何过渡动画：

```javascript
function reviewNext() {
  // ...
  renderReviewByStep(); // 直接渲染，无动画
}
```

**问题：** 单词之间瞬间切换，缺乏"这一轮结束了，下一轮开始"的心理节奏感。

**建议动画方案：**
- **正确回答**：卡片向左滑出 + 新卡片从右滑入（轻量，~200ms）
- **错误回答**：卡片轻微抖动 + 淡出 + 新卡片淡入
- **拼写正确**：绿色勾号弹出 + 缩放过渡
- **拼写错误**：左右抖动（已有 `.spell-wrong { animation: fbShake }`，可复用）

动画原则：
- 时长控制在 150-250ms（不长到让人等待，也不短到没感知）
- 使用 `transform` + `opacity`（GPU 加速，不触发 reflow）
- 尊重 `prefers-reduced-motion`（无障碍）

---

#### 问题 C：反馈不够"有分量"

当前反馈机制：

| 事件 | 反馈 | 评价 |
|------|------|------|
| 点击"认识" | 直接跳到释义页 | ❌ 无反馈 |
| 点击"模糊" | 直接跳到释义页 | ❌ 无反馈 |
| 点击"不认识" | 直接跳到释义页 | ❌ 无反馈 |
| 拼写正确 | 绿色文字 + "✓" | ⚠️ 有但轻微 |
| 拼写错误 | 红色文字 + 正确答案 | ⚠️ 有但轻微 |

**核心问题：判断按钮点击后没有任何即时反馈**，用户不知道系统是否接收了他的判断。

**建议方案：**

**方案一：按钮按下后的即时视觉反馈**
```css
.review-btn:active {
  transform: scale(0.97);  /* 已有 */
  opacity: 0.88;           /* 已有 */
}
/* 补充：点击后短暂变色 + 禁用，防止重复点击 */
.review-btn.pressed {
  filter: brightness(1.2);
  pointer-events: none;
}
```

**方案二：释义页加入判断结果的视觉印记**
- 如果刚才点了"认识"：释义页左上角显示淡绿色竖条
- 如果刚才点了"模糊"：释义页左上角显示淡橙色竖条
- 如果刚才点了"不认识"：释义页左上角显示淡红色竖条

这已经在部分实现了（`.review-stat.just-known/fuzzy/unknown`），但只是统计块的轻弹，不够显眼。

---

#### 问题 D：进度显示方式可优化

当前：`3 / 15`（数字比例）

**问题：**
- 只显示"已完成/总数"，不显示"还剩多少"
- 当数量大时（如 50 个），数字变化慢，成就感弱

**建议方案：**
1. **顶部细进度条**（视觉化进度，比数字更直观）
2. **分段显示**：如"第 3 词，共 15 词 · 预计 3 分钟"
3. **完成时的里程碑反馈**：每完成 25% 显示一个轻量 toast（"已完成 25%！"）

---

### 2.3 TTS 播放反馈优化

#### 当前 TTS 行为：

| 阶段 | TTS 行为 | 评价 |
|------|---------|------|
| 判断页 | 自动播放 2 次 | ⚠️ 可能泄露答案（前文已述）|
| 释义页 | 手动播放（扬声器按钮）| ✅ 合理 |
| 拼写页 | 手动播放（扬声器按钮）| ✅ 合理 |

**建议优化：**
1. 判断页 TTS 改为**手动触发**（已在 1.1 节详述）
2. 释义页显示后**自动播放 1 次**（帮助建立音义联系）
3. 增加**播放状态视觉反馈**：播放时扬声器图标变为波形动画

---

## 三、重新设计 Review 流程

### 3.1 当前流程的问题总结

```
当前流程：
单词 → [TTS自动播2次] → 认识/模糊/不认识 → [释义+历史统计] → [拼写] → 下一个
         ↑ 问题1                ↑ 问题2           ↑ 问题3      ↑ 问题4
      泄露答案             无强制回忆          信息过载     总出现
```

### 3.2 推荐的新流程设计

```
┌─────────────────────────────────────────────────────────────┐
│                    新 Review 流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐                                               │
│  │ ① 回忆页  │  单词居中大字显示                              │
│  │          │  提示："想想它的意思"                           │
│  │          │  底部细线渐进填充（3秒思考时间）                 │
│  │          │  扬声器图标（手动点击播放）                      │
│  │          │                                               │
│  │          │  ────────────                                  │
│  │          │  [认识]  [模糊]  [不认识]  ← 思考时间后或随时可点│
│  └────┬─────┘                                               │
│       │                                                      │
│       │  用户点击判断                                         │
│       ▼                                                      │
│  ┌──────────┐                                               │
│  │ ② 确认页  │  单词 + 音标                                   │
│  │          │  释义（根据判断显示不同细节）                     │
│  │          │  TTS 自动播放 1 次                              │
│  │          │                                               │
│  │          │  ────────────                                  │
│  │          │  [记错了]  [继续]                               │
│  └────┬─────┘                                               │
│       │                                                      │
│       │  仅当判断为"认识"时                                    │
│       ▼                                                      │
│  ┌──────────┐                                               │
│  │ ③ 拼写页  │  "请拼写这个单词"（不显示中文释义）              │
│  │          │  输入框                                        │
│  │          │                                               │
│  │          │  ────────────                                  │
│  │          │  [提交]  [跳过]                                 │
│  └────┬─────┘                                               │
│       │                                                      │
│       │  拼写错误 → 降级为"模糊"并显示正确答案                  │
│       │  拼写正确 → 保持"认识"                                │
│       ▼                                                      │
│   下一个单词                                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 各阶段详细设计

#### 阶段①：回忆页（核心改进）

**设计目标：最大化主动回忆强度**

```
┌─────────────────────────┐
│                         │
│                         │
│       abandonment       │    ← 38px/700 weight，居中
│                         │
│    🔊                   │    ← 扬声器图标，灰色，手动点击
│                         │
│   想想它的意思...        │    ← 13.5px/text-3，提示文字
│                         │
│   ──────────────        │    ← 底部细线，3秒内从左到右填充
│                         │
│   [认识]  [模糊]  [不认识]  ← 填充完成后高亮，但也可提前点击
│                         │
└─────────────────────────┘
```

**关键改动：**
1. **移除自动 TTS** → 改为手动
2. **添加 3 秒思考引导** → 底部细线渐进（非倒计时，无焦虑）
3. **按钮始终可见但"未激活"态** → 提示"可以先想想"
4. **移除所有历史数据和统计** → 纯净的回忆环境

**交互细节：**
- 用户可以在思考时间内提前点击（不强制等待）
- 思考时间结束后，按钮有一个微弱的"脉冲"动画提醒
- 如果用户 10 秒未操作，不做任何事（避免压力）

---

#### 阶段②：确认页（精简版）

**设计目标：验证判断 + 强化记忆，不干扰**

```
┌─────────────────────────┐
│  3 / 15                 │    ← 进度（小字，顶中）
│                         │
│       abandonment       │    ← 单词，26px
│       /əˈbændənmənt/   │    ← 音标，14px/text-2
│                         │
│  n. 放弃；抛弃           │    ← 释义，15.5px
│                         │
│  ┌─────────────────┐    │
│  │ ▎ 记住了         │    │    ← 左侧彩色竖条表示本次判断
│  └─────────────────┘    │    （绿=认识/橙=模糊/红=不认识）
│                         │
│   🔊                    │    ← TTS 自动播放 1 次
│                         │
│   [记错了]    [继续]     │    ← 操作按钮
│                         │
└─────────────────────────┘
```

**关键改动：**
1. **移除历史统计**（认识X次/模糊X次/不认识X次）→ 这些数据放到完成页
2. **移除查询次数上标** → 无意义信息
3. **添加左侧彩色竖条** → 本次判断的视觉印记
4. **TTS 自动播放 1 次** → 在确认阶段帮助音义绑定
5. **"记错了"按钮保留** → 元认知修正机制有价值

---

#### 阶段③：拼写页（条件触发）

**设计目标：检验"真认识"还是"假性熟悉"**

**触发条件：仅当用户判断为"认识"时才进入拼写**

```
┌─────────────────────────┐
│  3 / 15                 │
│                         │
│   请拼写这个单词         │    ← 提示文字（不显示中文释义！）
│                         │
│   ┌─────────────────┐   │
│   │                  │   │    ← 输入框，18px/600 weight
│   └─────────────────┘   │
│                         │
│   🔊                    │    ← 手动播放（帮助回忆发音）
│                         │
│   [提交]      [跳过]     │
│                         │
└─────────────────────────┘
```

**关键改动：**
1. **不显示中文释义** → 这才是真正的回忆测试
2. **拼写错误后果**：自动将本次判断降级为"模糊"（影响记忆曲线）
3. **拼写正确后果**：维持"认识"判断，给予正向反馈
4. **"跳过"按钮**：允许用户跳过（偶尔不想拼），但跳过不计错误
5. **对"模糊"和"不认识"的词**：直接跳过拼写，进入下一词

---

## 四、减少认知负担

### 4.1 当前页面信息审计

#### 判断页（`renderReviewCard`）当前显示的信息：

| 信息 | 是否必要？ | 建议 |
|------|-----------|------|
| 进度（3/15）| ✅ 必要 | 保留，但可缩小 |
| 单词 | ✅ 必要 | 核心 |
| 扬声器按钮 | ✅ 必要 | 改为手动 |
| "想想它的意思，然后如实判断"| ✅ 必要 | 保留 |
| 认识/模糊/不认识按钮 | ✅ 必要 | 核心 |

**结论：判断页已经很干净 ✅**（主要问题是 TTS 自动播放）

#### 释义页（`renderReviewDetail`）当前显示的信息：

| 信息 | 是否必要？ | 建议 |
|------|-----------|------|
| 进度（3/15）| ✅ 必要 | 保留 |
| 单词 + 查询次数上标 | ⚠️ 部分必要 | **移除上标** |
| 音标 | ✅ 必要 | 保留 |
| 释义行 | ✅ 必要 | 保留 |
| **认识/模糊/不认识累计次数** | ❌ **不必要** | **移至完成页** |
| "记错了"按钮 | ✅ 必要 | 保留 |
| "下一步"按钮 | ✅ 必要 | 保留 |

**结论：释义页有 2 项冗余信息（查询次数上标、历史统计）❌**

#### 拼写页（`renderReviewSpell`）当前显示的信息：

| 信息 | 是否必要？ | 建议 |
|------|-----------|------|
| 进度（3/15）| ✅ 必要 | 保留 |
| "根据释义拼写单词"提示 | ⚠️ 部分 | 改为"请拼写这个单词" |
| **中文释义** | ⚠️ **视情况** | **拼写时不显示（提高难度）** |
| 输入框 | ✅ 必要 | 保留 |
| 确认/下一个按钮 | ✅ 必要 | 改为"提交/跳过" |
| 拼写结果 | ✅ 必要 | 保留 |

**结论：拼写页的中文释义降低了回忆难度 ❌**

### 4.2 信息重新分配方案

```
判断页：    单词 + 进度 + 提示 + 按钮          （极简）
释义页：    单词 + 音标 + 释义 + 判断印记 + 操作  （精简）
拼写页：    提示 + 输入框 + 操作               （无释义）
完成页：    本次统计 + 历史统计 + 详细数据       （集中展示）
```

**核心原则：复习时只关注"我记不记得？"，其他一切数据事后再看。**

---

## 五、记忆强度模型优化

### 5.1 当前模型

```
三档：认识 / 模糊 / 不认识
↓
映射到阶段推进或降级
↓
决定下一次复习间隔
```

**局限：** 无法区分"立即想起"和"想了很久才想起"。

### 5.2 推荐的四维信号模型

不改变用户界面（仍然是三个按钮），但在后台记录更多维度：

| 维度 | 数据来源 | 用途 |
|------|---------|------|
| **显式判断** | 用户点击的按钮 | 主信号（权重 60%）|
| **反应时间** | 单词显示到点击的时间差 | 辅助信号（权重 25%）|
| **拼写结果** | 拼写正确与否 | 验证信号（权重 15%）|
| **历史趋势** | 最近 N 次判断的方向 | 平滑信号（防抖动）|

#### 反应时间的具体应用

```javascript
// 伪代码：增强版 answerReview
function answerReviewEnhanced(word, result, responseTimeMs) {
  const st = reviewStore.words[word];
  
  // 如果用户判断为"认识"但反应时间 > 5秒
  // 说明这不是真正的"认识"，降级处理
  if (result === "known" && responseTimeMs > 5000) {
    result = "fuzzy";  // 内部降级，用户无感知
  }
  
  // 如果用户判断为"模糊"但反应时间 < 1.5秒
  // 说明可能是"不认识"但不好意思承认
  // 可以缩短下次间隔（保守处理）
  if (result === "fuzzy" && responseTimeMs < 1500) {
    st.nextAt = businessDayAt(1, now);  // 明天再复习（而非 2 天后）
  }
  
  // ...原有逻辑...
}
```

**用户无感知**：界面不变，只是后台更聪明。

### 5.3 记忆状态可视化（完成页）

在完成页面，不只是显示"认识 X / 模糊 X / 不认识 X"，而是显示更有意义的信息：

```
今日复习完成 ✓

本次复习 15 个单词

┌─────────────────────────────┐
│  🟢 牢固掌握    8 个词       │
│     这些词将在 7 天后再次出现 │
├─────────────────────────────┤
|  🟡 需要巩固    5 个词       │
│     这些词将在 2 天后再次出现 │
├─────────────────────────────┤
|  🔴 仍需学习    2 个词       │
│     这些词明天将会再次出现   │
└─────────────────────────────┘

坚持就是胜利！🎯
```

这比冷冰冰的数字更有激励作用，也更直观。

---

## 六、拼写机制重新审查

### 6.1 当前设计的矛盾

代码注释明确说：
> "拼写不参与记忆曲线，纯附加强化"

但同时：
- 拼写**总是出现**（无论什么判断）
- 拼写**必须正确才能过关**（错误后须重新输入）
- 拼写**提供了中文释义提示**（降低了难度）

这三个特性组合在一起，导致拼写变成了一个**"形式主义的抄写任务"**，而非有效的记忆强化手段。

### 6.2 推荐的新拼写策略

#### 核心原则：拼写是"认识的试金石"

```
判断为"认识" → 进入拼写 → 拼写正确 → ✅ 真认识，维持原判
                          → 拼写错误 → ⚠️ 假性熟悉，降为"模糊"

判断为"模糊" → 跳过拼写 → 下一个（先解决语义记忆）

判断为"不认识" → 跳过拼写 → 下一个（先解决语义记忆）
```

#### 为什么这样设计？

1. **只有"认识"才需要验证**：如果用户已经说"模糊"或"不认识"，拼写没有意义（他连意思都不知道，怎么可能拼对？即使拼对了也是靠字母碎片记忆）。

2. **拼写错误应该有后果**：如果用户说自己"认识"但拼不出来，这说明他是"假性熟悉"（只认得长相，不会写），这种记忆是很脆弱的，应该降级。

3. **不显示释义提高纯度**：拼写时不显示中文释义，这才是真正的"从记忆中提取拼写"。如果显示了释义，那就变成了"看中文写英文"的短时记忆测试。

#### 拼写错误的处理流程

```
用户提交拼写
    ↓
正确？
    ├─ 是 → 显示"✓ 拼写正确"（绿色，带轻弹动画）
    │     → 1.5秒后自动进入下一词
    │
    └─ 否 → 显示正确拼写（红色，带抖动动画）
          → 显示"再试一次"（而非"请重新输入"）
          → 用户可选择：
              a) 重新拼写（输入框预填空，聚焦）
              b) 跳过（接受降级为"模糊"）
```

#### 连续拼写失败的熔断机制

如果一个词连续 2 次拼写错误：
- 本次不再要求拼写（避免挫败感）
- 标记该词为"需重点拼写训练"
- 在后续复习中，该词的拼写间隔单独计算（比语义间隔更频繁）

---

## 七、实施优先级建议

### P0（必须做，效果最大）

| 改动 | 工作量 | 效果 |
|------|--------|------|
| 判断页移除自动 TTS | 改 1 行代码 | 消除答案泄露 |
| 释义页移除历史统计 | 删除 ~8 行 HTML | 消除锚定效应 |
| 拼写改为仅"认识"触发 | 加 1 个 if 条件 | 减少无效拼写 |

### P1（强烈推荐，提升体验）

| 改动 | 工作量 | 效果 |
|------|--------|------|
| 判断页加 3 秒思考引导 | CSS + JS ~20行 | 强制主动回忆 |
| 拼写错误影响记忆曲线 | 修改 answerReview 逻辑 | 消除假性熟悉 |
| 拼写页不显示中文释义 | 删除 1 行 | 提高拼写纯度 |
| Review 进入沉浸模式（隐藏TabBar）| CSS ~5行 | 提升专注感 |

### P2（体验加分，可迭代）

| 改动 | 工作量 | 效果 |
|------|--------|------|
| 单词切换过渡动画 | CSS keyframes ~30行 | 节奏感 |
| 反应时间后台记录 | 修改 onReviewAnswer | 更精细的间隔 |
| 完成页重新设计 | 修改 renderReviewDone | 激励感 |
| 进度条替代数字 | CSS + JS ~15行 | 直观感 |

---

## 八、总结：核心理念转变

### 当前设计的隐含假设：
> "用户是诚实的，他们会认真看每个词，如实判断，然后从释义中学习。"

### 更符合实际的假设：
> **"用户会疲劳、会捷径、会高估自己的记忆。系统的责任是引导真实的回忆，而不是信任用户的自觉。"**

### 改进方向总结：

1. **从"提供答案"转向"引导回忆"**：减少提示，增加回忆时间
2. **从"信任判断"转向"交叉验证"**：用拼写验证"认识"的真伪
3. **从"信息分散"转向"信息集中"**：复习时极简，完成后详尽
4. **从"普通页面"转向"专注空间"**：视觉和交互的沉浸感设计
5. **从"单一信号"转向"多维信号"**：判断 + 反应时间 + 拼写的综合评估

---

*本报告基于 VocabHit v0.7.13 源码分析，结合认知心理学中的主动回忆理论、测试效应、遗忘曲线、编码特异性、加工深度等原理撰写。*
