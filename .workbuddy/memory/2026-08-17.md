# 2026-08-17 — VocabHit v0.7.14 Review 复习界面现代化重设计

## 范围
- 仅表现层 + 交互层；Review 业务逻辑、记忆曲线、数据结构、本地存储未触动
- review-active 模式下：隐藏 TabBar、悬浮查词、页头标题/清空；保留单词/按钮区
- 单词舞台中央悬浮 + 极淡紫色环境氛围光；46px 现代粗体（11 字默认、13-15 lg、16-19 md、20+ sm）
- 三枚软色药丸按钮（认识/模糊/不认识），r 18px，色 13/橙/红透明底+饱和文字，scale 0.95 反馈
- 扬声器 38px 圆形，scale 0.88→1 轻反馈
- 屏幕切换：旧屏淡出 110ms → 新屏淡入 300ms + 14px 上推（prefers-reduced-motion 降级）
- 释义页：词性 chip + 释义文本；左侧 4×30px 短色条作为上轮判断印记
- 拼写页：状态化按钮组 [跳过+确认] / [跳过+重新提交] / [下一个]；补全了原本缺失的「拼写正确后下一步」交互（绑定已有 spellNext 逻辑，无新业务代码）
- 完成/过渡页：取消卡片堆叠，三列纯文字统计带细分隔线，去除 emoji

## 关键修复
- `.app.review-active` 选择器 bug（class 实际在 body）→ 改为 `body.review-active`（TabBar/标题/清空/悬浮查词均隐藏）
- 清理 `.review-stat / .spell-word / rvFadeIn / rvSlideUp / fbPop / fbShake` 残留死代码
- preview-review.html 同步注入 `vc-records`，避免 `syncReviewWithWords` 清掉测试数据

## 验证
- 用 headless Chrome（puppeteer-core）跑 4 组视口：390×844 浅色、360×640 小屏、844×390 横屏、390×844 深色
- 全流程：注入数据 → 词义(5 词判断) → 过渡 → 拼写(5 词错+对) → 完成
- 测试脚本要点：直接 `el.click()` + 显式 450ms 等待越过应用内 300ms 操作冷却（避免 puppeteer actionability 与动画竞态）

## 版本 & 备份
- versionCode 42 / versionName "0.7.14"（android/app/build.gradle + index.html 设置页）
- 备份：D:\Vocabulary\VocabHit-v0.7.14-2026-08-17\（src/ + APK + README.txt）
- Debug APK：VocabHit-0.7.14-debug.apk（18.97 MB，已构建）

# 2026-08-17 — VocabHit v0.7.15 删除内部悬浮条 / 历史记录 UI / Review 数字同步

## 任务 1：删除软件内部悬浮查询条
- index.html：移除 #floating-widget DOM + 设置页「悬浮查词」开关（保留 #overlay-item 后台悬浮查词）
- app.js：移除 applyFloating、floatPanel/Input/Result/Clear 常量、renderFloatDetail/openFloatPanel/closeFloatPanel/updateFloatClear 函数、5 处引用（refreshFlow/switchTab/__back/Escape/focusin）、startup applyFloating 调用
- style.css：清理 .floating-widget / .float-* 全套样式（含 body.review-active .floating-widget 隐藏规则）
- **保留** overlay.html + #overlay-item + #overlay-switch + applyOverlay/initOverlay（外置 Overlay 后台悬浮查词）

## 任务 2：查询记录 UI 重构
- 单词 → 释义 → 查询时间 三行纵排全部左对齐，视觉层级递弱
- 查询时间左对齐至记录区域左边缘，紧凑单行「YYYY/MM/DD HH:mm」，font-size 11px、tabular-nums
- 移除 history-dot；释义省略号截断；长单词自然换行（overflow-wrap: break-word）
- 小屏 ≤375px：压缩 padding/font，12px 起
- 端到端视觉对比：旧版时间在右对齐、缩进 13px 错位 → 新版时间稳定靠左、每条记录布局统一

## 任务 3：Review 数字状态同步（修复了真实 bug）
- **真实 bug**：完成页分支 `clearReviewSession(); saveReviewSession();` 在清空后又把 phase:"done" 写回 localStorage，导致 App 重启后再点 Review 入口仍显示「今日复习完成」完成页，与首页「今日无需复习」状态不一致
- 修复：完成页仅 `clearReviewSession()`（不 save）；首页数字始终实时取自 `reviewQueue().length`，不维护独立缓存
- 11 项 puppeteer 自动化断言全通过：
  - S1 完成全部 Review → 首页归零（count=今日无需复习, queue=0）
  - S2 完成后会话已清除、重进显示空状态（key=已移除）— **修复验证**
  - S3 中途退出显示剩余（3/5）
  - S4 清空队列按生词本重建（5）
  - S5 删除生词同步（4/4）
  - S6 重启前后数字一致
  - S7 跨业务日 nextAt 实时计算 + 重启不缓存

## 验证脚本（puppeteer-core）
- rv-count-verify.js：Review 数字同步 7 场景
- rv-regress-verify.js：v0.7.15 回归（悬浮条移除/历史 UI/搜索/设置页/Review）
- rv-hist-shot.js：历史 UI 视觉截图（浅色/小屏/深色）

## 版本 & 备份
- versionCode 43 / versionName "0.7.15"（android/app/build.gradle + index.html 设置页）
- 缓存戳：app.js?v=12 / style.css?v=8
- 备份：D:\Vocabulary\VocabHit-v0.7.15-2026-08-17\（src/ + APK + README.txt）
- Debug APK：VocabHit-0.7.15-debug.apk（18.97 MB）