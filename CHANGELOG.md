# 更新日志

## v0.9.17（2026-08-26）
坚持看板 · 柱状图数值标签改为每柱单独标注：

**一、标签显示方式：组合数字 → 每柱独立数值**
- 现象：两根并列柱顶部共用一个组合标签（如 5/1、19/1），不够直观，需要心算拆分才能对应到具体柱子。
- 修改：红色词汇柱顶部单独显示词汇数量，蓝色知识点柱顶部单独显示知识点数量；每个数字水平居中于对应柱子顶部正上方（.chart-val 的 text-anchor: middle 保证）；柱高不同时两个数字各自跟随本柱柱顶定位，一高一低互不干扰；不再显示 5/1 等组合格式。
- 单柱日（仅词汇或仅知识点）行为不变：仅在该柱上方标注对应数字。
- 柱子数据、颜色、尺寸、布局、日期轴与折线图等其余统计功能零改动。

**二、版本信息**
- 版本 1.0.7（versionCode 80）；缓存戳 app.js ?v=21；根目录与 Android assets 同步，无数据结构改动。


## v0.9.16（2026-08-26）
首页词典搜索与 Math Lab 串 UI 根绝（作用域硬门控）：

**一、根因与修复：从「被动回收」升级为「主动门控」**
- 根因：Math Lab 的浮层（网格/坐标轴开关菜单、预设操作面板、命名对话框）是全局 fixed 元素，与词典搜索面板共用 .sheet 类、共享 bottom: var(--ime)。既有防御只有「事后回收」（MutationObserver 检测离开后关闭），存在时序缺口：任何路径残留的 .open/.visible 状态若在回收判定之外被唤起（初始化恢复、焦点/键盘交接、异步时序），键盘弹出时 --ime 变化会把残留面板顶到键盘上方显形——这正是首页搜词时偶发看到网格开关等 Math Lab 设置界面的机制；另外菜单关闭时实验名称输入框未 blur，焦点残留造成跨模块键盘交接。全库审计确认不存在泛化选择器/全局事件误触 Math Lab 的路径，首页搜索链路（openSheet/输入/结果更新/文档级监听）与 Math Lab 的 DOM、状态、事件零共用。
- 修复：新增 body.ml-lab-scope 作用域类，由 mathLab.js 依据 Math Lab 三页激活态实时同步（页面切换即更新）；CSS 硬门控：非作用域内 #ml-menu-sheet / #ml-psheet / 两个遮罩 / .ml-dlg-mask 一律 visibility:hidden + pointer-events:none——无论何种路径与时序残留状态，Math Lab 浮层在首页等其他页面物理不可见、不可交互、不可聚焦，从渲染层面根绝串层；键盘弹出、--ime 变化、resize、搜索结果更新等一切首页场景均不可能唤起 Math Lab UI。
- 清理补全：forceCloseAllLayers 增加浮层内焦点元素统一 blur；closeMenu 关闭菜单时立即 blur 实验名称输入框；初始化时同步作用域并强制关闭全部浮层（含 WebView 状态恢复场景的启动前残留）。
- 未禁用任何全局事件：resize/键盘/focusin 等全局监听原样保留，其他模块键盘适配零影响；Math Lab 页面内（作用域开启）函数图像、网格开关、缩放、拖动、参数调整等功能零变化；首页词典搜索/悬浮搜索零改动。
- 验证：node --check 语法通过；根目录与 Android assets 两份副本哈希一致同步。

**二、版本信息**
- 版本 1.0.6（versionCode 79）；缓存戳 math-lab.css ?v=4、mathCanvas.js ?v=4、mathLab.js ?v=4；无数据结构改动。


## v0.9.15（2026-08-25）
统计图表数学一致性修复（柱状图标签居中 + 折线图单一纵轴）：

**一、柱状图标签：对齐较高柱，修正悬殊日视觉右偏**
- 现象：5/0 等单柱日标签已居中，但 19/1 这类悬殊日（红柱高、蓝柱矮）标签仍视觉右偏——标签的 y 锚定在较高柱顶，x 却对柱组中心，视觉主体是高柱，显得歪斜
- 修复：标签改为对齐较高柱的顶部正中央（哪根柱高就对哪根）；双柱近乎等高（差 ≤ 2px，视觉对称）时对柱组中心；单柱日仍对齐该柱；保留既有 1px 左移修正
- 柱子尺寸、颜色、布局零改动；5/0 等已对齐的日期不受影响（规则不变）

**二、折线图：双刻度各自归一 → 单一纵轴（数学事实修正）**
- 现象：累计与每日各用一套刻度归一，导致失真——累计 2.8 > 每日 1.4 两线却交汇于同一点；同为 1.4 的两个值却画在不同高度（每日线被顶到左侧刻度 2.8 的位置、视觉上明显高于累计线），与左侧参考数字对不上，不符合数学事实
- 修复：两条线共用同一纵轴刻度（取两序列最大值归一）——等值必然同高、累计 ≥ 每日必然不低于每日、线条位置与左侧参考刻度严格一一对应；右侧原独立的第二套刻度数字移除（单一纵轴只保留左侧一组参考值，避免两套数值误导）
- 「累计复习时长」实线与「每日复习时长」虚线的样式、点选交互、数值标注逻辑均不变；柱状图/统计卡片零改动

**三、版本信息**
- 版本 1.0.5（versionCode 78）；缓存戳 app.js ?v=20；根目录与 Android assets 同步，无数据结构改动

## v0.9.14（2026-08-25）
坚持看板 · 统计图表微调（柱状图标签 + 折线图点选）：

**一、柱状图标签格式与居中微调**
- 柱顶数量格式由「5·1」改为「5/1」：前数 = 词汇数量，后数 = 知识点数量（红柱仍代表词汇、蓝柱仍代表知识点，颜色不变）
- 标签对准实际绘制柱体的顶部正中央：双柱日对齐柱组中心，单柱日（仅词汇或仅知识点）对齐该柱自身中心（此前单柱日标签固定在槽位中心，视觉上偏右）；在此基础上再整体左移 1px 修正残余视觉偏右
- 仅位置与文案微调：柱子尺寸、颜色、图表布局零改动

**二、折线图新增点选交互（近 7 天每日学习时长）**
- 每天对应一条整列透明命中区（含数据点，触屏易命中）：点击某天 → 数值标注切换为该日数据（累计 = 实线当日值，日期标注 = 虚线当日学习时长），数据点、日期、时长一一对应；点选往期日期同样准确
- 默认显示今日数据的逻辑保留（未点选时标注为「累计 X / 今 Y」，与原版一致）；点击其他日期即切换，点击图表以外任意位置恢复今日显示，反复切换/恢复均正常
- 点选反馈：选中日数据点放大、日期文字强调、新增虚线引导线（均沿用现有图表配色体系）
- 切换仅重绘折线图 SVG 自身（replaceWith），不重渲染整块看板，无滚动跳动；「累计复习时长」实线与「每日复习时长」虚线的绘制逻辑零改动，柱状图/统计卡片/其余统计功能零改动

**三、版本信息**
- 版本 1.0.4（versionCode 77）；缓存戳 app.js ?v=19、style.css ?v=12；根目录与 Android assets 同步，无数据结构改动

## v0.9.13（2026-08-25）
拼写模式 IME 回车键修复：

**一、Android 输入法「回车/下一步」= 点击「确认」（消除界面上移）**
- 根因：拼写输入框未处理 IME 动作键，回车/下一步触发 WebView 默认行为（焦点后移 / 聚焦元素滚入视野），拼写屏可滚动容器被顶动 → 界面整体上移/跳动
- 修复：#spell-input 监听 keydown，命中回车（兼容 e.key / keyCode 13 / which 13，适配显示「回车」「下一步」「完成」的各类输入法）先 preventDefault 拦截默认行为，再触发 #spell-submit.click() —— 与「确认」按钮走完全相同的委托链路（同一 300ms 防抖 + 同一 submitSpell 判定），无第二套判断机制：拼写正确直达下一题，错误停留当前词提示重输（按钮变「重新提交」）
- 输入法组词中的回车（isComposing / keyCode 229，候选词确认）不拦截，避免误提交未上屏文本；提交后同步 + rAF 双重复位滚动，杜绝个别 ROM/输入法仍把页面整体上移
- 输入框新增 enterkeyhint="done"：引导输入法展示「完成/确认」类按键；键盘全程保持显示（不 blur、不禁用 resize/viewport 适配），正确进下一题后自动聚焦新输入框
- 监听仅绑定拼写模式的 #spell-input；查词、错题、知识条目等其他输入框的回车行为零改动，词义复习等其余 Review 交互零改动
- node --check 语法校验通过；根目录与 Android assets 两份 app.js 哈希一致同步（缓存戳 app.js ?v=18）

**二、版本信息**
- 版本 1.0.3（versionCode 76）；仅拼写输入交互变更，无数据结构/间隔算法改动，存量数据无迁移风险

## v0.9.12（2026-08-22）
拼写复习交互修复 + Math Canvas 真全屏：

**一、拼写复习「确认」直达下一题（取消「下一个」中间态）**
- 修复错误流程：输入 → 确认 → 翻页但仍停留当前词 → 按钮变「下一个」→ 再点一次才进下一题
- 新逻辑：「确认」= 提交 + 判断 + 正确即前进 —— 拼写正确立即进入下一单词并播放「滴」提示音（audio/next.mp3），无需再点任何按钮；「确认」按钮始终承担提交判断职责
- 拼写错误：不进入下一题，保持当前单词，展示正确拼写并提示重新输入（按钮临时变「重新提交」），再次确认重新判断；错误提交不重排队、不触发提示音
- 仅拼写模式变更；词义复习等其余 Review 交互零改动（「下一个」按钮与提示音绑定关系不变）

**二、Math Canvas 三级页真全屏（消除顶部白边）**
- 根因：沉浸模式下 .app 容器仍保留顶部安全区 padding（--sat），画布整体下移产生顶部白边
- 修复：body.ml-canvas-active 下清除 .app 顶部/底部 padding，#page-math-canvas 以 100dvh 铺满整屏，画布从屏幕物理顶端开始绘制
- 顶部悬浮条上移紧凑化：top = 安全区 + 3px（原 8px 间隙），坐标读数同步上移；不遮挡系统状态栏的前提下视觉更紧凑
- 拖动/缩放/双击复位等画布交互零影响；纯布局与安全区调整，不做裁切伪全屏

**三、验证**
- test_review_v0911.js 更新拼写交互断言（E3 错误停留重输、正确即前进、无「下一个」中间态；E4 正确提交循环推进；G1 错误提交不播、正确提交播一次），全套 77 项通过
- 全量回归 459 项通过（SR 142 + stats 45 + srUI 47 + mathlab 99 + repo 49 + libraries 39 + highlight 38）
- 浏览器端到端：拼写错误停留/正确前进/提示音/跳过/完成页全流程通过；Math Canvas 顶部 y≈0 无白边、悬浮条 top=3px、高度铺满视口、拖拽缩放正常
- 版本 1.0.2（versionCode 75）；缓存戳 app.js ?v=17、math-lab.css ?v=3


## v0.9.11（2026-08-22）
生词 Review 三项增强（既有间隔算法零改动）：

**一、下一题提示音**
- 词义复习与拼写复习中，点击「下一个」进入下一单词时立即播放短促「滴」提示音（audio/next.mp3，用户提供的音频文件）
- 每进入下一条播放一次；判断、提交、跳过等其余操作均不触发
- 独立 Audio 实例，与既有 TTS/在线发音通道互不影响；自动播放策略拦截时静默降级

**二、当天队列「队末重排」机制**
- 词义复习：某词被判「模糊/不认识」时不立刻结束，放回当天复习队列队尾再次出现，直到最终「认识」通过；当天队列彻底走完才算本轮完成（当天复习次数可超过单词数）
- 拼写复习同样采用：跳过（未通过）的词放回拼写队列队尾再次出现，直到拼写正确通过；提交错误不重排（原地重新输入直至正确或跳过）
- 拼写队列基于当天词集合去重后随机打乱（Phase 1 重排产生的重复项不带入，每词只考一次起步）
- 过渡页/完成页文案标注总判断次数（如「已完成 5 个单词的词义复习（共判断 6 次）」）

**三、当天首次判断锁定长期间隔结果（核心）**
- 当天重复考察 ≠ 新的一次长期间隔复习：每个词当天第一次判断（认识/模糊/不认识）即锁定为其长期 Review 基准，后续重复判断只决定「今天是否继续排队」，绝不改写长期状态
- 例：C 首次「不认识」→ 按原有「不认识」算法安排下次复习；C 当天再次出现答「认识」仅使其出队，明日仍按「不认识」对应间隔复习
- 「记错了」双分支：首次判断的修正回滚并按「不认识」重算（仍属当天首次基准）；重复判断的修正仅改当天判断与统计，长期状态零触碰
- 既有长期 Review 算法（认识/模糊/不认识的自适应间隔曲线）完全保留，零修改

**四、会话状态与兼容**
- 复习会话新增当天会话字段 answeredToday（已做首次判断的词）与 initialCount（初始词数），与长期 Review 状态彻底分离
- 旧版会话数据恢复兼容：缺失字段兜底补齐，行为不劣化
- 会话持久化含重排队列与锁定状态，中断后可恢复到「队末重排待二次出现」的位置

**五、验证**
- 新增 76 项测试通过（算法回归/队末重排/首次锁定/记错了修正/拼写队列/会话持久化/提示音触发面/存储与通道隔离）
- 全量回归 459 项通过（SR 142 + stats 45 + srUI 47 + mathlab 99 + repo 49 + libraries 39 + highlight 38）
- 浏览器端到端验证：队末重排、首次判断锁定、提示音播放（11 次）、去重拼写队列、完成页文案、会话清理全部通过
- 版本 1.0.1（versionCode 74）；缓存戳 app.js ?v=16


## v0.9.10（2026-08-21）
「选择知识加入复习队列」页两项修复：

**一、选中条目闪白 Bug（根因修复）**
- 根因：每次勾选/取消勾选走 renderLibPage() 整体重建 #sr-pick-list 的 innerHTML，且每行 .sr-pick-row 带入场动画（animation-fill-mode:both，from 态 opacity:0），重建后全部行重播动画 → 整屏闪白 + 滚动位置丢失
- 修复：勾选改为局部更新 —— 只切换对应行的 on class 与圆圈 checked 态、更新「已选 X 条」计数与浮条显隐、同步全选按钮文案；列表 DOM 节点零替换、动画零重播、滚动与其他条目状态原样保留
- 全选/取消全选同样改为局部更新；连续多选流畅稳定

**二、考察方式设置页完整展示知识条目**
- 此前只显示「原文文本」；现完整展示 Repository 原始条目的全部已有信息：原文 + 中文解释 + 上下文（带独立小标签区分）
- 提问型设置预设问题/答案、挖空型选择挖空内容时，均可依据完整信息操作（挖空源文本仍基于完整原文）
- 数据直接读取 vc-repo 原始条目字段，不重建精简数据；条目无解释/上下文时对应区块优雅隐藏
- 数据一致性：加入队列后 state 只保存考察配置（examType/examConfig），不复制、不覆盖、不简化知识内容；vc-repo 原样零写入，原文/解释/上下文完整保留
- 复用既有展示样式（sr-rv-explain / sr-rv-context，与复习卡片一致）

**三、验证**
- 新增 47 项测试通过（局部更新/DOM 稳定性/多选/全选/完整展示/空字段隐藏/批量翻页/数据一致性/存储隔离）
- 全量回归 412 项通过（SR 142 + mathlab 99 + repo 49 + libraries 39 + highlight 38 + stats 45）；浏览器端到端验证零报错
- 版本 0.9.10（versionCode 73）；缓存戳 srUI.js ?v=6、spaced-repetition.css ?v=6

## v0.9.9（2026-08-21）
坚持看板新增「统计」区（真实本地数据聚合，零模拟）：

**一、统计卡片**
- 坚持看板最底部新增「统计」卡片，两排四项：今日复习（X词/X条）、累计复习（X词/X条）、今日总时长（X分钟）、累计时长（X分钟）
- 「大数字 + 小单位」视觉层级：数字大号加粗、词/条/分钟小号弱化，tabular-nums 对齐
- 数据全部来自真实本地记录：生词复习（vc-review 答题上报 + 历史基线）、知识条目复习（vc-spaced-repetition answer 钩子上报）、学习时长（复习有效时长 + 专注番茄 vc-pomo-records）

**二、柱状图（复习数量 · 近 7 天）**
- 每日双柱：红 = 词汇复习数、蓝 = 知识点复习数，组顶标注「词·条」
- 纯 SVG 渲染，今日日期高亮；无记录日为 0 不虚造

**三、折线图（学习时间 · 近 7 天）**
- 实线 = 累计复习时长（按每日结束净值永久连续累加，不随 7 天窗口重置）、虚线 = 每日复习时长
- 双刻度各自归一，末端标注当前累计/每日值；学习时长 = 复习有效时长（单次封顶 3 分钟防挂机）+ 专注番茄时长

**四、数据体系（最小化扩展）**
- 新增独立命名空间 vc-stats：{ v, base(历史基线冻结), days(逐日明细) }；首次初始化以既有 vc-review/vc-spaced-repetition 总量冻结基线，防重复计数，不回写任何既有键
- 业务日统一 04:00 边界（与 vocabDay 一致）；近 7 天仅为图表滚动窗口，窗口外历史完整计入累计
- 复习答题（生词 + 知识条目）逐笔上报；stats.js 缺失或无钩子时优雅降级，SR 模块零副作用

**五、验证**
- 新增统计模块测试 45 项通过（基线冻结/逐日记账/时长结算/业务日边界/近 7 天窗口滚动/累计连续性/持久化/存储隔离/SR 钩子）
- 全量回归 367 项通过（SR 142 + mathlab 99 + repo 49 + libraries 39 + highlight 38）；浏览器端到端验证空态/数据态渲染与数字核对通过
- 版本 0.9.9（versionCode 72）；缓存戳 stats.js ?v=1、app.js ?v=15、srData.js/srUI.js ?v=5、style.css ?v=11

## v0.9.8（2026-08-21）
生词 Review 体验优化（仅表现层，算法零改动）：

**一、翻书换页动画**
- 单词切换（回忆 → 结果反馈 → 下一单词）统一采用「翻书换页」过渡：旧屏以左缘为轴向右轻旋揭走，新屏从左侧旋入就位
- 实现方式：旧屏克隆为不透明「翻出页」盖在新屏上翻走，新屏全程在场 —— 绝无白屏、闪烁或跳变
- 轻微克制：约 280ms、小角度透视（无夸张 3D、无纸张卷曲）；支持 prefers-reduced-motion 自动关闭

**二、回忆页自动发音**
- 回忆模式（Phase 1 词义复习）进入新单词自动播放一次英语发音（延迟约 120ms，出现即播）
- 发音链路沿用现有「本地 TTS 优先，在线（有道）兜底」逻辑，未做修改
- 立即中断：点判断按钮答题、点下一个、跳过/拼写切词时 ttsStop() 立即停掉当前音频，不允许播完
- 拼写模式保持不主动播放；本次未加入答对音效

**三、存储库页 UI 微调**
- 「已在队列 / 未在队列」副标题（X 条正在间隔重复 / X 条未加入）与切换控件增加呼吸间距，不再贴挤
- 空库时移除列表为浮条预留的底部空隙，「该库暂无知识条目」空状态框上移贴近切换控件

**四、验证**
- 全量回归 367 项通过（SR 142 + mathlab 99 + repo 49 + libraries 39 + highlight 38）
- 版本 0.9.8（versionCode 71）；缓存戳 app.js ?v=14、style.css ?v=10

## v0.9.7（2026-08-21）
「自适应间隔算法」v2.1：间隔设置回归 + 库页同页切换：

**一、间隔设置（回归，非模板概念）**
- 新增「间隔设置」入口（存储库复习页）：六档间隔自定义 — 第一次复习 / 第二次 / 第三次 / 第四次 / 第五次 / 第六次及以上（天）
- 默认预设值与生词 Review 当前默认完全一致：首复习次日（1 天），认识推进 3→7→10→20→30→每月（30 天）
- 两套算法完全独立：存储库间隔重复自有规则（存 vc-spaced-repetition.intervals），生词 Review 算法与数据零影响
- 修改仅影响后续调度，已在队列条目的既有排期不追溯；支持「恢复默认」
- 非「复习模板」概念：单一规则直接修改，无创建/选择/绑定模板

**二、库页「已在队列 / 未在队列」改为同页 Segmented 切换**
- 由上下两个大模块改为同一页面内的选项切换（复用设置页外观/生词视图同款 .segmented/.seg-btn）
- 两选项同排一行、当前项高亮、点击立即切换列表内容，无页面跳转
- 两状态数据逻辑不变：一条目只属于其一；「未在队列」勾选 → 加入复习 → 自动切到「已在队列」立即看到
- 进入库页自适应落点：有复习中条目 → 「已在队列」，否则 → 「未在队列」
- 修改考察方式后返回原页签（已在队列）；底部「加入复习」浮条仅未在队列页签且勾选时出现

**三、验证**
- Node 测试：SR 模块 142 项（间隔设置默认值/校验/自定义调度/不追溯/损坏回退/持久化 + 上版全部）+ 既有回归 225 项全部通过
- 版本 0.9.7（versionCode 70）；缓存戳 srData.js/srUI.js ?v=4、spaced-repetition.css ?v=5

## v0.9.6（2026-08-21）
「自适应间隔算法」v2 重构：复习模板概念移除，改为每条知识独立的间隔重复 + 考察方式：

**一、概念重构**
- 「复习模板」功能整体取消（入口/名称/UI 全部移除），统一为「间隔重复」
- 间隔改为固定六档 1·3·7·14·30·60 天（记得进档 / 模糊减半 / 忘记回退次日 / 已掌握退出）
- v1 数据自动迁移 v2：既有复习进度（阶段/次数/时间）完整保留，模板与库绑定数据丢弃

**二、存储库页面两区划分**
- 库页明确分为「已在队列」「未在队列」两区，条目绝不重复出现
- 已在队列：显示内容、考察方式、当前阶段、下次复习时间；点击可查看/修改考察方式（不动复习进度）
- 未在队列：圆圈批量选择（○→✓）+ 全选，点击「加入复习」逐条设置考察方式

**三、每条知识独立的间隔重复**
- 每条条目独立拥有：加入时间/复习阶段/上次复习时间/下次复习时间/复习次数/考察方式
- 首次复习按各自加入时间计算，不同时间加入的条目互不共享复习日期
- 考察方式归属每条条目（同一库可混用提问型/挖空型），可随时修改

**四、考察方式（第一阶段两种）**
- 提问型：用户预设问题与正确答案；复习时显示问题 → 自行回忆 → 记得/模糊/遗忘 → 显示预设答案 → 下一条
- 挖空型：复用荧光笔「原生选区→偏移→切换」逻辑在原文中选择隐藏部分；复习时填写 → 系统判分（✓ 正确 / ✗ 错误）→ 显示正确答案 → 下一条
- 判分容错：忽略大小写与多余空白；全对=记得 / 部分对=模糊 / 全错=遗忘
- 旧数据（未设置考察方式）自动走原有「提示→显示答案→判断」兜底流程

**五、其他**
- 设置页「自适应间隔算法」入口移除图标，文字与其他设置项对齐
- 生词 Review 与存储库间隔重复完全独立（零写入既有键）

**六、验证**
- Node 测试：SR 模块 v2 120 项（考察方式校验/挖空判分/每条目独立调度/v1→v2 迁移/数据隔离）+ 既有回归 225 项（mathlab 99 + repo 49 + libraries 39 + highlight 38）全部通过
- 版本 0.9.6（versionCode 69）；缓存戳 srData.js/srUI.js ?v=3、spaced-repetition.css ?v=4

## v0.9.5（2026-08-21）
修复「自适应间隔算法 → 存储库 → 选择知识」页底部操作栏 Bug（纯 CSS 三处，零功能改动）：

**一、问题根源（三因叠加）**
- `[hidden]` 失效：`.sr-pick-bar` 的 `display:flex` 压过浏览器默认 `[hidden]{display:none}`，0 选中时操作栏也立即显示
- `pageIn` 动画的 transform 含块效应：进页 300ms 内 `.page` 带 `translateY`，成为 fixed 子元素的定位基准 → 操作栏先按页面内容底部定位（看似正确）；动画结束 transform 消失 → 瞬间跳回视口底部，产生「先正常显示 → 闪动 → 跑到底部被 Tab 栏遮挡」
- 停靠点落在 Tab 栏内部且层级更低：`bottom: 18px+安全区` 位于 Tab 栏（高约 60px+安全区）区域内，`z-index: 40 < 70` 被完全遮挡

**二、修复**
- 操作栏重定位：`bottom: calc(84px + --sab)`（Tab 栏上方 24px 间隙）、`z-index: 71`（高于 tabbar 70，低于 sheet 90 / toast 100）、居中限宽（同 .tabbar 模式，桌面端收进 430px 取景框）
- 显式恢复 `.sr-pick-bar[hidden]{display:none}`：未选中时正确隐藏
- `#page-sr-lib` 改用纯 opacity 淡入（srLibFade）替代带 transform 的全局 pageIn：定位基准全程不变，彻底消除闪动（无任何 setTimeout 类 Hack）；reduced-motion 同步
- 列表底部预留经核算充足：`.sr-pick-list` padding-bottom 90px + 页边 24px + .app 为 Tab 栏预留 72px+安全区 = 186px > 操作栏顶部（84px+安全区+高约 61px），末条不被遮挡

**三、验证**
- 浏览器端到端实测：操作栏 bottom(702) 与 Tab 栏 top(725) 间隙 23px；连续多选位置数值不变；滚动到底末条 bottom(600) < 操作栏 top(643)；返回上级不残留；重新进页无闪动
- Node 测试全通过：SR 模块 92 + 既有回归 126（repo 49 + libraries 39 + highlight 38）
- 版本 0.9.5（versionCode 68）；缓存戳 spaced-repetition.css ?v=3

## v0.9.4（2026-08-21）
自适应间隔算法（全新模块：生词计划只读展示 + 存储库知识复习，既有生词 Review 零修改零耦合）：

**一、入口与页面层级**
- 设置页底部新增「🧠 自适应间隔算法」入口（副标题：管理生词与存储库的复习计划）
- 二级页两模块并列：生词（当前有 XX 个词正在复习）/ 存储库（当前有 XX 条知识正在复习）

**二、生词模块（只读复用，零写入）**
- 「生词复习计划」直接读取现有 Review 队列：单词 + 下次复习时间（今天需复习/今天稍后/明天复习/X天后复习）
- 完全复用现有生词 Review 状态与调度逻辑，本模块零计算零写入，nextReview 原样透传
- 点击单词弹出既有释义卡片（复用现有查词卡片；列表改用 data-sr-word，避免触发全局查询统计）

**三、存储库复习（独立知识复习系统）**
- 存储库条目默认不进复习队列：进入库 → 点圆圈勾选（复用每日任务圆圈交互）→「加入复习」
- 支持单选/连续选择/批量选择/取消，微信聊天记录式轻量批量操作，无多余确认流程
- 每个存储库可绑定一个复习模板；条目加入队列时自动继承库当前模板
- 复习状态完全独立（reviewEnabled/reviewStage/lastReviewAt/nextReviewAt/reviewCount/templateId…），与生词 Review 数据零共享

**四、复习模板（用户可 DIY）**
- 内置「经典间隔」示例模板：1·3·7·14·30·60 天
- 模板管理：新建/修改/删除/设为默认；六档间隔自由调整（1–365 天，第六档 = 长期重复档）
- 删除模板：库绑定与条目自动回退默认模板，已复习进度不重置；修改模板：仅影响后续复习间隔

**五、主动回忆复习页**
- 好句：隐藏整句，中文解释/上下文作提示；短语：显示中文回忆英文；句型：遮蔽关键结构保留结构词与首字母；搭配：显示首词回忆完整搭配；自定义类型：隐藏内容以解释作提示
- 「显示答案」→ 自我判断：记得（进入下一阶段，stage 6 封顶长期重复）/ 模糊（间隔减半保守延长，最少 1 天）/ 忘记（回退一阶段次日复习）
- 「已掌握」随时退出复习队列（reviewEnabled=false，原始知识条目保留不删，可再次加入从头开始）

**六、数据与架构**
- 独立命名空间 vc-spaced-repetition（模板/库绑定/复习状态），绝不写入 vc-review 等任何既有数据键
- 模块化：js/spaced-repetition/（srData 数据层 + srUI 界面层）+ css/spaced-repetition.css；纯本地离线，零联网零 AI
- Node 自动化测试 92 项全通过（模板/绑定/阶段推进/出题规则/时间文案/数据隔离）；既有回归 126 项全通过（repo 49 + libraries 39 + highlight 38）
- 版本 0.9.4（versionCode 67）；缓存戳 spaced-repetition ?v=2

## v0.9.3（2026-08-21）
Math Canvas 沉浸式交互重构（参考 ChatGPT 数学可视化工具的产品思路，功能零删减）：

**一、极简沉浸画布**
- 整个屏幕 = 数学画布：图像占据绝大部分空间，无设置面板/缩放按钮/参数卡片/大型工具栏
- 顶部仅保留轻量浮动胶囊栏：返回 · 实验公式（可点击查看详情）·「⋯」更多菜单
- 底部工具栏全部移除：显示设置/导出/保存等次级操作收纳进「⋯」底部弹层菜单
- 手势提示（拖动平移 · 双指缩放 · 双击复位）首次进入浮现，任意交互即隐

**二、智能最佳视野（autoView）**
- 生成后自动分析函数特征（零点、极值点、积分区间、竖线、标注点）计算最佳初始视野，替代固定 [-10,10] 窗口
- y=x²-2x-3 → 两零点与顶点全部入画；y=x² → 单特征点自动扩展至抛物线主体；sin(x) → 完整周期居中
- 周期函数特征群组化截断（对称特征同进同退）；奇点（1/x）/定义域外（ln）不崩、安全回退
- 等比缩放取小方向铺满：内容完整可见且无夸张留白；重复实验恢复用户上次视图

**三、自然手势探索**
- 单指拖动平移、双指捏合缩放（以手势中心为锚点）、双击平滑复位最佳视野（easeOutCubic 动画，缩放几何插值）
- 轻点显示十字准线坐标读数，拖动即隐；双击即清并复位
- 鼠标拖动/滚轮缩放（桌面）与 ＋/− 键盘缩放保留

**四、其他**
- Math Lab 测试 99 项全通过（新增 D 组：autoView 视野覆盖 14 项 + analyzeCurve 特征检测 4 项）；既有回归 126 项全通过
- 版本 0.9.3（versionCode 66）；缓存戳 math-lab ?v=2

## v0.9.2（2026-08-21）
Math Lab 数学实验室（全新模块，既有功能零修改）：

**一、首页入口与页面层级**
- 首页新增「📐 Math Lab · 数学实验室」入口卡片（函数 · 导数 · 积分 · 极限 · 代数）
- 页面层级：Dashboard → Math Lab（主页）→ 实验配置 → Math Canvas（沉浸式数学画布三级页）

**二、实验配置（六种生成类型）**
- 函数：公式 + 可选标注点；导数：原函数 + 导函数曲线 + 指定点切线
- 不定积分：原函数族数值积分 + 常数 C 可调；定积分：上下限 + 积分区域面积直观显示
- 极限：趋近变量/目标（含 ±∞）/单双侧 + 数值估计；代数：表达式或方程求解并标注全部根
- 公式输入即时校验（✓/✕ 反馈）；支持隐式乘法（2x、x(x+1)）、pi/e 常数、^ 幂、常用函数
- 类型注册表架构：后续新增数学类型只需在 TYPES 注册一项

**三、Math Canvas 沉浸画布**
- 单指拖动平移、双指捏合缩放、鼠标拖动/滚轮缩放（桌面）、＋/− 按钮缩放、一键重置视图
- 十字准线 + 实时坐标读数（轻点固定）；网格/坐标轴显隐切换；底部悬浮工具栏
- 本地 Canvas 2D 实时绘图（自适应采样、断点处理、漂亮刻度 1/2/5×10ⁿ），非服务端图片
- 顶部悬浮公式条 + 信息条（积分值/切线斜率/极限结果/方程解）
- 沉浸模式自动隐藏底部导航；画布页失活自动恢复

**四、预设与最近实验**
- 我的预设：新建/打开/重命名/删除/更新，完全由用户管理；首次播种 3 个示例预设（可删不复活）
- 最近实验：签名去重（同配置不重复）、最新置顶（容量 100，优先淘汰未保存项）
- 打开最近实验完整恢复：数学内容 + 缩放比例 + 平移位置 + 显示设置
- 画布页「保存实验」：公式/类型/参数/显示设置/当前视图一并持久化

**五、PNG 导出**
- Math Canvas 菜单「导出图片」：离屏 1.6 倍高清渲染（含公式标题/信息/水印），复用原生导出管线保存到系统相册；Web 环境直接下载

**六、数据与架构**
- 独立命名空间 mathLab.presets / mathLab.experiments / mathLab.recentExperiments，与既有 vc-* 数据零污染
- 模块化：js/math-lab/ 七文件（engine/data/config/renderer/canvas/presets/lab）+ css/math-lab.css
- Node 自动化测试 81 项全通过（引擎 39 + 数据层 20 + 配置层 22）；既有回归 126 项全通过（repo 49 + libraries 39 + highlight 38）
- 版本 0.9.2（versionCode 65）；缓存戳 math-lab ?v=1

## v0.9.1（2026-08-20）
存储库数据隔离修复 + 重命名（库间同名类型彻底独立）：

**一、自定义存储库不再预设词条类型**
- 预设类型（短语/句型/好句）仅属于默认库；新建的自定义库为空，类型完全由用户新增
- 存量数据自动迁移（幂等，零丢失）：自定义库中曾存入预设类型的条目，自动转为该库同名自定义类型，内容/来源/时间完整保留
- 空类型库添加知识时直接展开「＋ 自定义」输入；未建类型保存会被拦截并引导先创建类型

**二、存储库重命名**
- 存储库页面长按库卡片 → 重命名（默认库与自定义库均可）
- 仅修改当前存储库名称，库内类型与知识条目等所有数据不受影响；与其他库重名会被拦截

**三、修复跨存储库数据串联（严重）**
- 删除类型的二次确认弹窗条目数统计、以及确认后的实际删除，均严格限定「当前库 + 当前类型」范围
- 删除自定义库的「好句」不再误读/误删默认库同名类型下的数据；不同库的同名类型彻底独立

**四、兼容性**
- 默认库既有数据、我的错题、Knowledge/Review/生词查询等全部功能不受影响
- 版本 0.9.1（versionCode 64）；缓存戳 knowledge.js?v=16；设置页版本显示同步

## v0.9.0（2026-08-20）
存储库上层架构 + 我的错题模块（Repository 之上新一级，既有功能零重写）：

**一、存储库一级页面（新增）**
- 首页「存储库」入口 → 存储库页面：默认库 / 自定义库 / 我的错题 并列展示，各卡片显示类型数·条目数
- ＋ 新建存储库：输入名称即可创建（如考研英语/高等数学），各库数据严格隔离互不混淆
- 存储库（一级）与类型（二级）严格区分：进入某个库 → 原 Repository 页面（类型筛选/搜索/添加/展开/编辑/删除/来源定位/来源高亮/长按删类型全部照旧）

**二、旧数据自动迁移（零丢失）**
- 升级后原 Repository 的全部数据自动归入「默认库」：条目/类型/原文/中文解释/上下文/来源/来源定位/添加时间无一项丢失
- 幂等迁移（vc-repo v1→v2）：可重复执行，不重复建库不复制数据；Node 自动化测试 37 项验证通过

**三、我的错题模块（新增，与知识库并列的独立学习模块）**
- 科目管理：＋ 添加科目自由命名（数学/英语/机械原理…），各科目错题完全独立；长按科目输入名称确认删除
- 错题字段：上传时间（自动记录）/ 错题原图 / 错题文字（可空）/ 正确解答（可选）/ 错因（可选）/ 思路（可选）；图片与文字至少存一项
- 错题列表：日期 + 标题 + 原图缩略图 + 错因摘要；错题详情页完整展示全部字段，可编辑可删除

**四、错题图片真持久化**
- 相册选择/拍照的原图由原生层复制到应用私有目录（filesDir/mistakes/），前端仅存 file:// 路径 —— 关闭/重启 App 后图片仍在
- 不依赖系统文档 Uri 临时权限；替换/删除错题时同步清理设备文件，不留孤儿；Web 预览回退 dataURL

**五、返回逻辑与兼容性**
- 页面栈：存储库→库(Repository)→条目 / 存储库→我的错题→科目→详情，左上角返回与系统返回键/全面屏手势同一逻辑
- 既有功能不受影响：Knowledge 解析、划重点、添加知识、来源定位/高亮、Review、生词查询、历史记录、坚持看板、悬浮条查询
- 版本 0.9.0（versionCode 63）；缓存戳 knowledge.js?v=15、knowledge.css?v=11；设置页版本显示同步

## v0.8.18（2026-08-18）
Knowledge 支持结构化解析 PDF / Markdown / 图片（统一文档模型）：

**一、统一文档模型（架构核心）**
- 所有文件类型（Word/PDF/Markdown/图片）统一转换为「行首控制字符标记」的结构化文档模型：标题(H1-H3)/段落/无序列表/有序列表/引用块/代码块/表格/聊天消息块/分隔线
- Knowledge 阅读页只渲染统一结构：阅读、划重点、添加知识、来源定位、来源高亮、Repository 提取全链路与既有体验完全一致
- 不再把任何文件压成一大段纯文本

**二、PDF 解析（新增，pdfbox-android）**
- 优先提取 PDF 自带文本层（绝不盲目 OCR）：字符级坐标收集 → y 聚类成行 → 词间空格重建（中文之间不误补空格）
- 双栏论文/教材：x 投影检测竖直空白带，左栏读完再右栏，杜绝 A D B E 交错；换栏处自动段落分隔
- 标题识别：字号 ≥1.35×正文中位 → H3、≥1.6× → H2、≥1.9× → H1
- 表格还原：连续 ≥3 行列分割点跨行对齐（±4pt）判定表格组，单元格以大间隙切分，结构化表格展示（列/行关系清晰、单元格自动换行、移动端适配）
- 段落分隔：相邻行基线差 > 1.7×中位行距自动空行；页与页之间空行分隔；超大文档保护（仅解析前 200 页）
- 扫描版/图片型 PDF（无文本层）：自动转 PdfRenderer 渲染位图（长边 2048px）→ ML Kit 逐页 OCR（上限 20 页，超出诚实告知）

**三、Markdown 解析（新增）**
- 识别 #/##/### 标题、-/*/+ 无序列表、1. 有序列表、> 引用、--- 分隔线、| | 表格、``` 代码块；行内标记（**粗体**、`代码`、[链接](url)）剥离为纯文字
- Web/原生双路径同一出口：ingestParseResult 按扩展名统一转换为结构化模型

**四、图片 OCR 结构重建（增强）**
- OCR 之上再分析结构：标题（行高 ≥1.9×中位 → H1、≥1.45× → H2，行宽受限防误判）、列表（项目符号/编号 → 原生列表样式）
- 聊天截图优化：User:/Assistant:/ChatGPT:/我:/对方: 等消息头发送者识别 → 消息块（发送者徽标 + 正文），保留消息结构不挤成一团
- 保留 ML Kit 原生行文本与版式（v0.8.9 基线），绝不重建空格/合并行，无文字粘连

**五、兼容与修复**
- Word 表格解析增强：单元格分隔/单元格内多段落（<br> 渲染）、标题样式段落标记
- 扫描版 PDF OCR 失败友好提示；文档选择器 MIME 扩展（pdf/markdown）
- 既有功能不受影响：划重点、高亮取消、添加知识、连续添加、来源定位、来源高亮、Repository 搜索、悬浮查词、Review、历史记录
- 版本 0.8.18（versionCode 62）；缓存戳 knowledge.js?v=14、knowledge.css?v=10；设置页版本显示同步

## v0.8.17（2026-08-18）
知识库学习模块 + 划重点 + 悬浮条任务优化：

**一、知识库学习模块（新增）**
- 支持导入本地 Word/Excel/PDF/TXT 文件，自动解析为知识条目
- 新增 DocumentParser 和 OcrParser 处理文档解析
- 知识库独立页面，支持浏览、搜索、筛选
- 资料详情页支持划重点、添加知识条目

**二、划重点功能（增强）**
- 资料详情页可划重点，支持跨段落/跨行/跨表格单元格
- 划重点架构重构：状态只存数据，DOM 仅是投影，真正可逆
- 支持取消划重点，按段落索引移除
- 旧格式数据自动迁移为偏移格式

**三、悬浮条每日任务（优化）**
- 支持设置每日任务生效时间区间
- 任务完成后自动隐藏悬浮条
- 修复悬浮条空状态残留残影

**四、Repository 类型删除（新增）**
- 长按类型按钮可删除整个类型及下属条目
- 二次确认弹窗，防止误删
- 预设类型删除后持久隐藏，重启不复活

**五、其他改进**
- 应用更名：从"词汇助手"更名为 VocabHit
- 重构查询记录 UI
- 修复 Review 数字残留问题
- 版本 0.8.17（versionCode 61）；设置页版本显示同步
## v0.8.16（2026-08-18）
划重点架构重构 + Repository 类型删除：

**一、划重点真正可逆（架构重写，非补丁）**
- 状态只存数据：高亮以 `{paraIndex, start, end}` 块内字符偏移区间持久化，DOM 仅是投影；滚动/重渲染/添加知识均不影响
- 增删按偏移判定：选区区间与已存区间求交 → 有交集整条移除，无交集新增；与文本内容无关，同一句子出现多处可独立高亮/取消
- 渲染无条件先解包：`clearUserHighlights` 后再按区间包裹，取消最后一条同样生效（修复旧实现早退守卫导致的视觉残留 → 永远无法取消）
- 包裹用「先插入锚点再移入节点」，文字内容/顺序/段落结构绝不改变（修复旧实现 `insertBefore(span, span.nextSibling)` 文字移位）
- 支持跨文本节点/跨行/跨表格单元格选区（`<br>` 两侧、多 `td` 之间）；相邻高亮合并渲染、部分重选整条取消
- 旧格式 `{paraIndex, text}` 数据打开资料时自动迁移为偏移，不丢失既有划重点
- Node 测试 38 项全部通过（可逆/同文多处/连续多区域/跨节点/部分重选/幂等重渲染/结构不变/旧数据迁移）

**二、Repository 删除整个类型**
- 长按类型按钮触发（600ms，滑动取消，附振动反馈）；普通点击仍为筛选，「全部」不可删
- 二次确认弹窗：明确告知将删除的类型名称与条目数，及「此操作不可恢复」；必须输入类型名完全一致才可点「永久删除」
- 级联删除：类型 + 该类型下全部知识条目（原文/解释/上下文/来源/时间），无孤立数据残留
- 预设类型（短语/句型/好句）删除后记入 `hiddenPresetCats` 持久隐藏，重启不复活；自定义类型直接从 `customCats` 移除
- 删除后立即同步：类型列表/条目列表/数量/搜索结果/首页角标；编辑面板默认分类自动退回首个可用类型
- 版本 0.8.16（versionCode 60）

## v0.8.15（2026-08-18）
取消划重点修复：
- **取消逻辑改为按段落索引移除**：不再依赖文本精确匹配，选区覆盖的段落内所有划重点一并移除（与真实荧光笔行为一致：选中的区域整段取消）
- **触发延迟缩短**：`onTextSelectEnd` 从 300ms 降至 150ms，提高移动端触发可靠性
- 版本 0.8.15（versionCode 59）

## v0.8.14（2026-08-18）
划重点 + 渲染全面修复：
- **移除列表检测渲染**：不再将 “1.” “-” 等开头的段落转为 `<ol>/<ul>`，彻底消除非连续列表项各自编号为 “1.” 的问题；所有文本均忠实以 `<p>` 渲染
- **表格数字不再折行**：`.kn-td` 的 `word-break` 从 `break-word` 改为 `normal`，仅在单词超长时才换行，短数字如 “10” “11” 不再被拆成两行
- **取消高亮修复**：`toggleHighlight` 中将 `root` 从选区的直接父元素改为向上查找 `#rd-text [data-i]` 段落元素，解决选区在 `.kn-hl-user` span 内部时 `querySelectorAll` 搜索不到外层 span 的问题
- **选区文本提取修复**：`getSelectionTextByPara` 用 `startContainer/endContainer` 精确判断边界节点，替代不可靠的 `compareBoundaryPoints` 逻辑
- **来源高亮不再干扰全局**：`highlightInPara` 末尾改为仅对当前段落调用 `applyHighlightsInPara`，不再触发全局 `applyHighlights` 重建所有段落
- 版本 0.8.14（versionCode 58）

## v0.8.13（2026-08-18）
划重点跨段落 + 列表渲染修复：
- **跨段落划重点修复**：新增 `getSelectionTextByPara`，将 Range 内文本按所属块元素拆分为独立段落，每段单独存储 `{paraIndex, text}`，解决大段选择时“整段文本存于起始段落导致后续段落高亮不匹配”的问题
- **有序/无序列表文本去重**：`<li>` 渲染时去除原文中的列表标记（`1. ` `2. ` `- ` `* ` 等），避免浏览器 `<ol>`/`<ul>` 自动编号与原文叠加产生 “1.1” “••” 等视觉重复
- 版本 0.8.13（versionCode 57）；设置页版本显示同步

## v0.8.12（2026-08-18）
划重点 + 表格渲染缺陷修复：
- **划重点文本消失修复**：重写 `toggleHighlight` 为纯数据层增删（不再直接操作 DOM 包裹 span），视觉高亮统一由 `applyHighlights` 重建；`applyHighlightsInPara` 每次执行前先解包已有 `.kn-hl-user` 并 `normalize()` 合并文本节点，保证幂等性；排序修复 `indexOf` 返回 -1 与 0 等价问题；新增 `donePositions` 集合防止同一位置重复包裹
- **表格单元格换行修复**：`extractCellText` / `DocumentParser.java` 将单元格内段落间 `\n` 及 `<w:br>` 产生的换行统一替换为 `\u001E`（Record Separator），避免破坏表格行分割；表格渲染器按 `/[\n\u001E]/` 拆分单元格内容并插入 `<br>`，保留单元格内多行结构
- **表格内容列表检测抑制**：单元格内段落不再添加 `\n`，从根源上避免 “1.” “2.” 等数字被分类器误判为有序列表
- **单元格标题标记过滤**：`extractCellText` 去除段落首部 `\u0001-\u0003` 标题标记，表格内不应有标题语义
- 版本 0.8.12（versionCode 56）；设置页版本显示同步

## v0.8.11（2026-08-18）
Knowledge / Repository 五项优化（划重点 · 表格渲染 · 交互分离）：
- **划重点功能**：阅读页正常阅读模式下，选中文本自动变为荧光笔高亮（柔和黄色，不影响可读性），再次选中已高亮文本取消高亮；高亮持久化存储于 localStorage，退出再进入同一资料仍然保留
- **取消自动查词**：移除正常阅读模式下“选中文本自动触发悬浮查词”机制（tryOverlayQuery 不再从 selectionchange 调用），查词功能统一通过既有悬浮条完成
- **交互模式严格分离**：正常阅读状态 = 划重点；添加知识状态 = 现有摘取流程（底部操作条确认）；两种状态互不干扰，退出添加知识后自动恢复划重点模式
- **Word 表格解析与渲染**：DocumentParser.java 新增 `<w:tbl>`/`<w:tr>`/`<w:tc>` 表格结构跟踪，前端 renderReaderText 识别表格标记渲染为真正的 HTML `<table>`，移动端可横向滚动、单元格长文本自动换行、首行加粗作为表头；Web 回退路径同步支持
- **Repository 页头布局对齐**：Repository 页头改用 flex 布局，与阅读页保持一致，返回按钮与“添加知识”按钮垂直居中对齐
- 版本 0.8.11（versionCode 55）；设置页版本显示同步

## v0.8.10（2026-08-18）
Knowledge / Repository 三项优化（阅读器结构化渲染 · 连续添加知识 · 手动创建条目）：
- **阅读器结构化渲染**：.docx 解析时保留 w:pStyle 标题层级（H1/H2/H3），前端 renderReaderText 识别标题标记渲染为语义化 `<h1>`/`<h2>`/`<h3>`，同时支持无序列表（`-` `*` `•`）和有序列表（`1.` `2)`）自动折叠为 `<ul>`/`<ol>`，每个元素保留 `data-i` 索引确保来源定位与高亮仍可用；原生路径（DocumentParser.java）与 Web 回退路径（paraTextOf）数据格式一致
- **连续添加知识**：在阅读页保存一条知识后自动回到内容选择态，可直接选择下一条内容继续添加，无需重复点击「＋ 添加知识」；底部操作条重新出现提示用户，点「取消」才退出；手动创建场景下不触发（currentMaterial 为 null）
- **Repository 手动创建条目**：Repository 页头新增「＋ 添加知识」按钮，点击打开无来源编辑面板（自动隐藏「从原文选择」按钮）；保存后条目 sources 为空数组，展开后来源区显示「手动添加」占位；既有来源定位/高亮/删除逻辑零改动
- **来源定位兼容新元素**：selectionchange 监听与 openReader 元素查找改用 `[data-i]` 属性选择器，标题与列表项均可参与来源定位
- 版本 0.8.10（versionCode 54）；设置页版本显示同步

## v0.8.9（2026-08-18）
Knowledge 图片 OCR 回归修复 · 第一阶段：恢复稳定基线 + 建立诊断记录（改动收敛于 OcrParser.java，Knowledge 工作流/UI/前端/交接协议零改动）：
- **根因定位**：v0.8.8 的空格/换行回归 100% 出在 OcrParser 后处理层（前端 knowledge.js 与交接链路对文本零加工，已排除）—— ① `buildLineText` 丢弃 ML Kit 原生 `line.getText()`（引擎返回文本本身含正确词间空格）改从元素 bounding box 间隙重建空格，阈值脆弱导致英文单词粘连；② `clusterRows`/`mergeRow` 按 centerY 跨块聚类行，摧毁 ML Kit 原生行边界导致行合并、换行丢失、多区域挤成一段
- **后处理回归 ML Kit 原生结构**（等价恢复 v0.8.7 稳定行为 + 安全增强）：行文本一律直接采用 `line.getText()`；行边界 = ML Kit 原生行边界（块内 `\n`）；块间按行间距判定段落（`\n\n`）或换行（`\n`）；彻底移除元素坐标重建空格与跨块行聚类两条路径
- **保守空格修复兜底**：仅当整行出现「连续 ≥8 个拉丁字母无任何空格」的病态输出时，才用元素间隙做空格修复，且修复后有效字符变少则保留原文（绝不更差）
- **保守双栏检测**：空白带阈值收紧至 ≥15% 页宽、两栏各 ≥4 行，且仅作块级阅读顺序调整，不再重组行内容
- **质量度量升级**：识别结果比较改用「Σ(行有效字符 × 行置信度 Line.getConfidence())」，旋转自检（90/180/270）与 Latin 重跑决策均基于新度量；连字符断词合并保留
- **图像预处理暂时停用**：v0.8.8 的灰度/光照平坦化/对比度拉伸从未经 A/B 实测，恢复基线不再调用（代码保留），待后续版本以「原图 vs 预处理图」质量自动取优方式重新验证
- **A/B 诊断记录**：每次 OCR 自动写诊断文件到 `getExternalFilesDir("ocr_debug")/ocr_<时间>.txt`（免权限，系统文件管理器可读）：A = 引擎原始 block/line 结构（原文/置信度/倾斜角/坐标框），B = 后处理最终输出；Logcat tag `VH_OCR` 同步 —— 用于区分问题发生在引擎识别层还是后处理层（C = Knowledge 阅读页显示，既有不变）
- 版本 0.8.9（versionCode 53）；设置页版本显示同步修正为 0.8.9（此前滞后于 0.8.2）

## v0.8.8（2026-08-18）
Knowledge 图片 OCR 识别质量专项优化（对齐成熟 OCR 软件体验，全程自动、零新增用户操作；改动收敛于 OcrParser.java + 新增 Latin 依赖，Knowledge 工作流/UI/前端零改动）：
- **环节定位**：旧实现三大短板 —— ① 无任何预处理（阴影/光照不均直进模型）② ML Kit 块顺序 = 检测顺序 ≠ 阅读顺序（段落错乱根因）③ 仅靠 EXIF 纠向，内容旋转时完全失效
- **自动方向纠正**：EXIF 元数据旋转 + 方向自检（有效字符 < 25 时依次尝试 90°/180°/270° 重识别，显著更优才采用，防噪声误切）—— 横拍、倒置、无 EXIF 图片自动转正
- **全自动预处理管线**：长边压至 2048（ML Kit 最佳识别区间，超大图反而降准）→ 灰度 → **光照场平坦化**（1/12 下采样估计背景亮度场并逐像素归一，阴影/光照不均/暗角/黑边自动洗白）→ 1%/99% 分位对比度拉伸；纯 Java 像素级实现，逐步释放中间数组控内存
- **阅读顺序重排（修复段落错乱）**：行内元素按坐标排序 + 间隙 >0.2×行高重建空格（修复英文词间空格丢失）→ 跨块视觉行聚类（y 中心容差 0.6×中位行高）→ **双栏检测**（48-bin 垂直投影空白带，允许 ≤12.5% 全宽行如标题跨越，两栏均需 ≥3 行防误检；先左栏后右栏）→ 段落按行间距 >0.6×行高判定 + 英文行尾连字符断词合并（"devel-"+ "opment" → "development"）
- **英文重点优化**：识别结果拉丁占比 ≥80% 且 ≥40 字母时自动切换 Latin 识别器重跑（新增 com.google.mlkit:text-recognition 16.0.0，仅 +1.5MB），字母/词间空格/英文标点精度更高；中英混排仍走中文识别器（占比阈值防止为中文牺牲英文）
- **健壮性**：识别超时 30s；无坐标返回自动回退旧输出格式；位图及时回收；失败仍走既有 OCR_EMPTY/失败提示，绝不进入空白阅读页
- 版本 0.8.8（versionCode 52）

## v0.8.7（2026-08-18）
新增 Knowledge 图片文字识别（OCR）导入：
- **统一导入入口**：Knowledge「＋ 导入学习资料」按钮改为弹出导入菜单（文档 .docx/.txt ｜ 图片 相册 JPG/PNG ｜ 拍照），三种来源汇入同一条「解析 → Knowledge 阅读页 → 摘取知识 → Repository」流程，不新建独立模块
- **图片 OCR（原生 ML Kit）**：新增 OcrParser.java —— ML Kit 中文文字识别（com.google.mlkit:text-recognition-chinese 16.0.0，项目首个也是唯一原生依赖），模型 bundled 打包进 APK 完全离线运行、无需 Google Play Services；中文识别器同时覆盖拉丁字母与数字 → 中英文混排一次识别；InputImage 自动按 EXIF 旋转校正，块/行按视觉阅读顺序输出（段落空行分隔，与 DocumentParser 输出一致）
- **相册 / 拍照双来源**：MainActivity 新增 pickImage（ACTION_OPEN_DOCUMENT image/jpeg+png）与 takePhoto（系统相机 ACTION_IMAGE_CAPTURE + cache/ocr 临时文件经 FileProvider 共享，OCR 后自动删除）；结果经 window.__onImageParsed 回传，与既有 __onDocParsed 同一 JSON 协议
- **复用现有解析链路与阅读页**：图片回调走同一 ingestParseResult（parseSeq 令牌防幽灵「解析中」），OCR 文本以普通学习资料形式持久化到 vc-materials —— 重新打开直接读已存文本，不再重复 OCR；不保存图片原件（相册 URI 不复制 / 拍照临时文件用后即删）；进入后完整使用现有阅读、添加知识、摘取、悬浮查词、来源回跳全部功能
- **错误处理**：未检测到文字 → 「未检测到清晰的印刷文字，请重新拍摄」；识别失败 → 提示换清晰印刷体照片重试；用户取消 → 静默复位；绝不进入空白阅读页。第一版边界：仅印刷体（中文/英文/数字/常用标点），不支持手写、潦草字迹、公式与复杂表格
- **Web 版**：图片/拍照入口明确提示「请在 Android App 中使用」（Web 无离线 OCR 与相机桥接），文档入口 Web 回退不变
- 版本 0.8.7（versionCode 51）；缓存戳 knowledge.css v4→v5 / knowledge.js v5→v6；root Web 副本同步

## v0.8.6（2026-08-18）
新增「中文释义反向查询」+ 生词页 UI 微调：
- **双向查询**：输入英文 → 查释义（原有路径零改动）；输入中文关键词 → 反向检索释义符合的多个英文词条（如「下降」→ decline/descent/descend…、「放大」→ magnify/amplify/enlarge…、「和谐」→ harmony…）。按输入内容自动分流（含汉字即走反查），无模式切换按钮；搜索框占位提示更新为「输入单词查释义 · 输入中文找单词」
- **匹配与排序**：关键词包含匹配（不要求完整释义，「下降」可命中「下降；减少；衰退」「使……下降」等）；匹配度排序 = 首释义完全等于 > 首释义以关键词开头 > 任一释义包含，同级按词频降序（考研高频优先）；无结果显示简洁空态
- **性能**：仅检索本地考研词书（6357 词），释义索引惰性构建一次常驻内存（首次 ~10ms），后续单次查询 <0.5ms，同步执行不卡输入；英文查询路径完全不变
- **完整复用现有链路**：反查结果行复用既有 .result-row UI；点击任一词条 → commitQuery → 现有详情页（完整释义/音标/播放读音/+1 生词本/今日·累计查询次数）全部原样；回车直接查询匹配度最高的词；混合输入（含汉字）走反查，纯英文不受影响
- **悬浮条同步**（软件外 = 应用内阅读页同一悬浮条）：悬浮条输入中文同样触发反查，结果行复用既有 .hit 列表（点击查详情、扬声器播放、查询计数全保留），三处查询体验一致
- **UI 微调**：生词页「X个生词 · 共X条查询记录」数量说明整体下移（与视图切换器拉开间距，观感更舒展，仅生词页生效）；反查结果顶部一行轻量提示「按中文释义找到 N 个单词」
- 缓存戳 style.css v8→v9 / app.js v12→v13；版本 0.8.6（versionCode 50）；root Web 副本同步（app.js / style.css / index.html / overlay.html）

## v0.8.5（2026-08-17）
UI 细节修复（仅阅读页返回按钮垂直位置）：
- **阅读页返回按钮对齐**：Knowledge / Repository 来源阅读页左上角返回按钮此前偏高 ~7px，与右侧「＋ 添加知识」按钮不平行 —— 根因是全局 .back-btn 自带 margin-bottom:14px，在 align-items:center 的阅读页头部中把按钮中心抬高。新增 `.kn-reader-header .back-btn { margin-bottom: 0 }` 覆盖（仅作用于阅读页头部，34px 尺寸/图标/点击区域/其他页面的 .back-btn 全部原样），两按钮在 flex 垂直居中下中心自然对齐，横竖屏及不同屏幕尺寸下均为同一 flex 布局规则，对齐一致
- 缓存戳 knowledge.css v3→v4；版本 0.8.5（versionCode 49）；root Web 副本同步

## v0.8.4（2026-08-17）
修复阅读页返回回退机制（按进入来源返回，不再固定回 Knowledge）：
- **entrySource 状态**：openReader 新增第 4 参 from（knowledge | repository），进入阅读页时明确记录；Knowledge 列表点击缺省 knowledge，Repository 来源点击传 repository —— 绝不按页面/文件名猜测上一页
- **统一返回函数 backFromReader()**：左上角返回按钮、系统返回键（MainActivity.onBackPressed → window.__back → handleModuleBack）、全面屏返回手势（同 onBackPressed 链路）三种方式最终汇聚同一函数、同一状态判断，行为完全一致；entrySource=knowledge → 回 Knowledge，entrySource=repository → 回 Repository，两入口彻底区分不串页
- **返回 Repository 完整还原状态**：点来源离开时快照（window.scrollY + 展开的卡片 id），返回时 renderRepo 后恢复展开手风琴并回滚滚动位置（抵消 switchTab 的 scrollTo(0)）；当前类型（repoFilter）与搜索状态（repoSearchKw + 输入框内容）为内存与 DOM 态，天然保留 —— 不重新进入 Knowledge，不丢筛选结果
- **临时状态优先级不变**：返回时先处理摘取模式（退出模式留在阅读页）→ 编辑面板（关面板留在阅读页）→ 再执行页面返回，任何情况不会因临时状态错误跳转 Knowledge
- Repository 来源场景其余功能原样：来源跳转 / paraIndex 定位 / 核心内容荧光高亮 / 悬浮条查询 / 普通阅读全部保留；VH_Knowledge.openReader 扩展点向后兼容（缺省 knowledge）
- 缓存戳 knowledge.js v4→v5；版本 0.8.4（versionCode 48）；root Web 副本同步

## v0.8.3（2026-08-17）
悬浮条查询扩展至软件内阅读页（复用同一系统级悬浮条，不新造查询组件）：
- **三场景统一**：软件外悬浮条 = Knowledge 原文阅读页悬浮条 = Repository 来源阅读页悬浮条 —— 同一 OverlayService 系统级窗口、同一 overlay.html UI、同一拖动/贴边吸附/防出屏、同一查询与生词记录（localStorage 同源互通），用户无「内部版」感知
- **实现原理（最小改动）**：系统级悬浮窗本就可覆盖应用内，唯一障碍是主应用前台时被隐藏 —— MainActivity 新增 overlayInApp 标志（onResume 据此决定显隐），前端进/出阅读页经 AndroidBridge.setOverlayInApp 通知；OverlayService 新增 query/collapse 指令注入
- **选中即查**：阅读页普通阅读态（非摘取模式）选中英文单词/短语（1~5 词，仅 #rd-text 正文选区，240ms 防抖）→ 悬浮条自动展开并查询该词（overlay.html 新增 window.__query 入口，复用既有输入查询管线：考研词典 + ECDICT 兜底 + 查询历史）；也可像软件外一样直接点悬浮条手动输入
- **与摘取模式彻底互斥**：selMode（添加知识/选解释/选上下文）期间选中文字绝不触发悬浮条，按既有摘取流程处理；反向进入摘取模式或编辑面板时自动收起悬浮条展开态（overlayCollapse），两种文字交互互不覆盖
- **与来源定位/高亮零冲突**：悬浮条为独立系统窗口，不触碰阅读页 DOM；知识点定位与荧光高亮不受查询过程任何影响
- **跟随既有开关**：应用内悬浮条与「设置 → 后台悬浮查词」同一开关（vc-overlay）；未开启时阅读页不出现悬浮条，选中单词仅一次性引导提示；无悬浮窗权限时静默跳过绝不崩溃
- 生命周期：退出阅读页 → 恢复「应用前台隐藏」既有行为；阅读页内切后台再回前台 → 悬浮条保持；Web 版（无 AndroidBridge）全部静默
- 缓存戳 knowledge.js v3→v4；版本 0.8.3（versionCode 47）；root Web 副本同步（knowledge.js / overlay.html / index.html）

## v0.8.2（2026-08-17）
Knowledge / Repository 增强（开闭原则不变：全部改动收敛于模块文件，既有定位/分类/展开/来源机制零破坏）：
- **来源定位后知识点荧光高亮**：从 Repository 点击来源 → 沿用既有 paraIndex 定位与滚动 → 在已定位段落内将该条目保存的「核心内容」以荧光笔样式高亮（空白容错匹配，跨空白差异不影响命中；核心内容被编辑过则自动退回整段闪烁，仍是正确位置）。高亮为纯 DOM 临时标记：每次打开阅读页重渲染即清除，绝不写入学习资料存储；不同条目点击来源各自高亮对应内容，互不残留
- **Repository 搜索**：新增搜索框，筛选管线为「Repository → 当前选中类型 → 关键词/时间 → 结果」：选中短语只搜短语、选中句型只搜句型、全部才搜全部；匹配范围为核心内容 + 中文解释 + 上下文 + 添加时间（如「2026」「8月17」「14:05」）；输入即时筛选（180ms 防抖），切换分类自动按「新类型 + 现有关键词」重新筛选，点 ✕ 或清空恢复当前分类全部；重新进入 Repository 自动清空搜索，避免残留筛选状态
- **搜索结果**：完全复用既有条目卡片（展开/收起、分类标签、来源跳转、删除全部保留）；无结果显示简洁空状态；匹配关键词以 `<mark>` 轻量高亮（核心内容/中文解释/上下文），不影响文字布局与来源定位
- **UI 微调**：Repository「X条知识条目」数量说明整体下移（与类型栏增加垂直间距，层级更清晰）；搜索框随分类联动显示「搜索「短语」的内容或时间」占位提示
- 缓存戳 knowledge.css/js v2→v3；版本 0.8.2（versionCode 46）

## v0.8.1（2026-08-17）
Knowledge / Repository 修复与交互重构（仍严格遵循开闭原则：app.js / style.css / 既有模块零修改，全部改动收敛于本模块文件）：
- **修复启动幽灵 UI**：`.kn-parsing` / `.kn-hintbar` / `.kn-newcat` 等类样式的 `display:flex` 覆盖了 UA 样式表 `[hidden]{display:none}`，导致带 hidden 属性的元素照样渲染 —— 于是冷启动首页就出现「选取解释」悬浮条、进 Knowledge 就显示「解析中」。已在 knowledge.css 对本模块元素补 `[hidden]{display:none!important}` 兜底（根因修复，非隐藏遮盖）
- **解析状态绑定真实任务**：新增 parseSeq 令牌 —— 只有用户真正发起上传后才接受解析回调；无活跃任务时迟到/重复回调直接丢弃；Web 路径改为选中文件后才置「解析中」；解析失败显示常驻错误条（.kn-error）而非一闪而过，下次上传自动清除；绝不凭空出现「解析中」
- **选择机制重构为「添加文本模式」**：普通阅读时长按 = 纯 Android 原生选择/复制，本模块零 UI 注入（不再与系统选择菜单叠加遮挡）；阅读页右上新增「＋ 添加知识」按钮作为唯一摘取入口，点击进入添加文本模式，底部出现固定操作条（提示「请选择要保存的知识内容」+ 取消/确认所选），确认所选才进入编辑面板 —— 两种交互状态完全隔离；退出模式自动清选区并恢复普通阅读
- **修复编辑弹窗巨型图标**：`.kn-pick-btn` 内联 SVG 无 class/尺寸，按替换元素默认 300×150 渲染撑爆弹窗；已补 `.kn-pick-btn svg{15×15}` 显式约束（根因修复，非整体缩放弹窗）
- **上下文改为用户主动选择**：删除自动截句生成上下文的逻辑（sentenceAround）；编辑面板新增「上下文 · 可选」字段（手动输入或「从原文选择上下文」），不添加则为空，Repository 详情不渲染空区域；字段层级固定为 核心内容 → 中文解释(可选) → 上下文(可选) → 来源 → 添加时间，无任何占位文案
- **状态生命周期清理**：selMode/pendingDraft/parseSeq 全部为内存态、不持久化；进入阅读页/切主 Tab/返回键/前后台切换均复位摘取模式与选区；App 冷启动永远是「普通阅读 + 无解析任务」干净状态
- 缓存戳 knowledge.css/js v1→v2

## v0.8.0（2026-08-17）
- 新增 Knowledge Learning / 学习资料与 Repository 模块（纯本地，严格按开闭原则实现：**app.js / style.css 及既有页面零修改**，新功能以自包含模块注入）：
  - **学习资料**：首页新入口「Knowledge · 学习资料」→ 上传 .docx / .txt（.doc/.pdf 明确提示不支持，宁缺毋滥）→「正在解析…」→ 解析完成自动进入独立二级阅读页；资料列表长期保存解析后纯文本，随时重读
  - **文档解析（原生）**：新增 DocumentParser.java（零第三方依赖）——.docx 走 ZipInputStream + XmlPullParser 流式提取 w:p/w:t/w:tab/w:br，段落顺序与空行原样保留；.txt 走 BOM 探测 → UTF-8 严格校验 → GBK 回退；后台线程解析，结果经 window.__onDocParsed 回调前端
  - **文档解析（Web 回退）**：js/knowledge.js 内置解析器注册表（registerParser 扩展点），TXT 走 TextDecoder 编码探测，DOCX 走纯 JS ZIP 目录解析 + DecompressionStream 解压（App 内则优先原生路径）
  - **独立阅读页**：现代移动端阅读器——吸顶玻璃返回栏、16.5px/2.02 行距排版、阅读时隐藏底部导航（沉浸式）；原文逐行渲染（textContent + pre-wrap），忠实保留段落、空行与空格，绝不改写、重排、合并
  - **文本选择**：复用 Android 原生长按选择手柄（可自由扩大缩小），选区上方浮现「＋ 加入 Repository」操作胶囊；支持单词/句子/任意连续文本
  - **条目编辑 Bottom Sheet**：核心内容（自动填入所选原文，可修改）+ 所属分类（预设：短语/句型/好句；支持「＋ 自定义」创建如考研作文/万能表达）+ 中文解释（可选：手动输入，或「从原文选择」回阅读页选取后自动带回，两个独立字段、原样保留）
  - **Repository 模块**：首页新入口，分类筛选（全部/预设/自定义）；知识条目卡片默认折叠（内容两行截断 + 分类标签 + 来源摘要），点击展开完整详情（核心内容/中文解释/原文上下文/来源/添加时间「年月日 时:分」）；手风琴单开 + grid 0fr→1fr 展开动画
  - **来源回跳**：条目来源芯片点击 → 返回对应学习资料阅读页并滚动定位到知识点所在段落（高亮闪烁提示）；资料已删除则仅提示，条目与来源快照完整保留
  - **数据独立（两个存储体系）**：vc-materials 与 vc-repo 完全解耦——删除学习资料不删条目（来源保留名称快照），删除条目不影响资料；二次确认删除均有明确提示
  - **重复检测**：同分类下规范化精确匹配才判重（不做模糊合并避免误合并不同记录）；命中时提示「该内容已存在于对应分类」并把新来源合并进既有条目（同一短语在 Day 3 与 Day 10 都出现 → 保留多来源）
  - **开闭原则落点**：既有 app.js/style.css/index.html 结构未动（index.html 仅追加 link/script 两个标签）；返回键采用装饰器包裹 window.__back；主 Tab 切换仅追加监听收尾浮层；对外暴露 window.VH_Knowledge 只读扩展点（未来复习系统等可直接接入）；MainActivity 仅追加 pickDocument 桥接与 onActivityResult 回传段（带独立注释块），既有导出/TTS/悬浮查词逻辑零改动
- 版本 0.7.15 → 0.8.0（versionCode 44）；缓存戳 knowledge.css?v=1 / knowledge.js?v=1
- 同步 android assets/www 副本（root Web 版与 www 保持一致）

## v0.7.15（2026-08-17）
- 删除软件内部悬浮查询条（首页已提供完整搜索面板 → 内部悬浮条重复且多余）：
  - index.html：移除 #floating-widget DOM 与设置页「悬浮查词」开关
  - app.js：移除 applyFloating、floatPanel/floatInput/floatResult/floatClear 常量、renderFloatDetail/openFloatPanel/closeFloatPanel/updateFloatClear 函数、相关事件监听器及 5 处引用（refreshFlow/switchTab/__back/Escape/focusin）
  - style.css：清理 .floating-widget / .float-* 全套样式
  - **保留** Android 外置 Overlay 后台悬浮查词（overlay.html + #overlay-item + #overlay-switch）
- 重构查询记录 UI：
  - 单词 → 释义 → 查询时间 三行纵排，全部左对齐，视觉层级递弱
  - 查询时间左对齐至记录区域左边缘（不再缩进），紧凑单行，完整保留「YYYY/MM/DD HH:mm」
  - 释义超出宽度时省略号截断；长单词自然换行
  - 移除 history-dot 干扰元素；新增 ≤375px 小屏压缩规则
- 修复 Review 首页「待复习数量」残留旧状态的真实 bug：
  - 全部完成 Review 时，原代码 `clearReviewSession(); saveReviewSession();` 在清空后又把 phase:"done" 的会话写回 localStorage，导致 App 重启后再点 Review 入口仍会显示「今日复习完成」完成页，与首页「今日无需复习」状态不一致
  - 修复：完成页仅 `clearReviewSession()`（不 save），会话彻底清除；首页数字始终实时取自 reviewQueue()，不维护独立状态
- Review 数字同步全场景自动化验证（puppeteer 11 项断言全通过）：
  - 完成归零 / 重进显示空状态 / 中途退出显示剩余 / 清空队列重建 / 删生词同步 / App 重启一致 / 跨业务日 nextAt 实时计算
- 同步 android assets/www 副本；缓存戳 app.js?v=12 / style.css?v=8

## v0.7.14（2026-08-17）
- Review 复习界面现代化重设计（沉浸式两阶段保留，仅优化表现层与交互层，未触动复习算法/记忆曲线/数据结构/本地存储）：
  - 顶部：极细进度条 + 轻量计数（"1 / 5"），半透明玻璃质感返回按钮悬浮于左上，隐藏"Review"标题与底部 TabBar/悬浮查词，形成独立学习空间
  - 单词舞台：单词悬浮于页面中央，充足上下留白与极淡的紫色环境氛围光；46px 现代粗体（11 字符以下默认、13-15 lg、16-19 md、20+ sm 智能降级），紧邻小型圆形扬声器
  - 判断区：认识/模糊/不认识 三枚软色药丸按钮等宽并排（绿/橙/红透明底 + 饱和文字），现代圆角 18px，按压 scale 0.95；底部安全区适配
  - 扬声器：点击 scale 0.88→1 的极轻反弹反馈（应用prefers-reduced-motion 降级）
  - 屏幕切换：旧屏淡出（110ms）→ 新屏淡入 + 14px 上推（300ms），不抢节奏
  - 释义页：词性 chip + 释义文本的清晰层级（按词性分行、释义自然换行），左侧 4×30px 短色条作为上轮判断的轻反馈
  - 拼写页：状态化按钮（未提交/错误→[跳过+确认/重新提交]，正确→[下一个]）——补全了原本缺失的「拼写正确后的下一步」交互路径
  - 过渡/完成页：无卡片堆叠的三列纯文字统计（带细分隔线），取消 emoji；适配 IME 的拼写底部操作区
- 修复：Review 沉浸选择器 `.app.review-active` 误写（class 实际挂在 body 上），TabBar 与"Review"标题在沉浸模式下未隐藏——改为 `body.review-active`
- 修复：清理 .review-stat / .spell-word / rvFadeIn/rvSlideUp 等残留死代码
- 预览注入页（preview-review.html）同步注入 vc-records，避免 syncReviewWithWords 清掉测试数据
- 响应式：小屏（≤375px）压缩字号、扬声器、控件；横屏（≤600px 高）启用滚动 + 紧凑布局
- 同步 android assets/www 副本；缓存戳 app.js?v=11 / style.css?v=7

## v0.7.13（2026-08-17）
- 修复番茄钟全屏运行时底部导航栏仍悬浮在时钟上方且可点击（z-index 层级）：运行中可误切页面导致计时器无头运行、沉浸式状态栏在其他页面保持隐藏
- 修复 Review「记错了」在原判断为模糊/不认识时会话统计扣错分桶（始终扣「认识」数）
- 修复 Review 快速连点跨步骤误触发：判定→释义→拼写三步底部按钮位置重叠（认识↔记错了、下一步↔下一个），双击第二击会落在下一步视图的同位置按钮上（如把「认识」悄悄改成「不认识」、跳词）。新增 300ms 操作冷却 + 步骤门控，正常节奏不受影响
- 修复后台悬浮查词确认的生词需重启 App 才进入 Review 队列：回到前台时同步执行生词本↔Review 同步与补建
- 修复导出「今日生词」元信息误用全部历史查询数：改为按业务日（04:00 切换）统计当日真实查询条数
- 修复 Web 预览 PNG 导出固定 900px 高度截断长生词列表：先离屏测量真实内容高度（APK 原生导出路径本已测量，不受影响）
- 修复查询记录页长历史全量渲染卡顿：仅渲染最近 500 条（数据永久保存不变），底部标注总条数
- Review 释义页增加三种判断结果的差异化轻反馈（认识绿/模糊橙/不认识红统计块轻弹）；首页统计数字变化轻弹、任务勾选 ✓ 轻弹、拼写正确/错误轻弹/轻摇（均支持 prefers-reduced-motion 降级）
- 同步 android assets/www 副本；缓存戳 app.js?v=10 / style.css?v=5

## v0.7.4（2026-08-16）
- 应用更名为 **VocabHit**（原"生词本"）
- 修复奖励表单"关联目标"偶现显示错乱（select 切换改 change 事件）
- 词典查询面板不再在任务表单输入时意外弹出（三层防御）
- 优化每日任务表单说明文字与"结束日期"选项的间距

## v0.7.3（2026-08-16）
- 修复奖励关联目标偶现 bug、任务输入偶现弹出词典界面

## v0.7.2（2026-08-16）
- 修复每日任务表单"结束日期"说明文字与选项重叠

## v0.7.1（2026-08-16）
- 坚持看板进入默认选中今天并显示当天任务，点击日期实时联动
- 每日任务支持开始/结束日期生效区间
- 删除每日任务改为软删除：只停止未来生成，历史完成记录全部保留
- 目标/单日任务日期禁止选择过去（动态校验）
- 切换页面自动关闭词典搜索面板（不再叠加）

## v0.7.0（2026-08-16）
- 坚持看板 UI 优化："今天"小字入当前日期格子、图例单行、月份标题快捷选年月

## v0.6.9（2026-08-16）
- 坚持看板支持上/下月切换、点击日期查看当天任务
- Review 拼写页"确认 / 下一个"合并为一行

## v0.6.8（2026-08-16）
- 修复悬浮窗旧数据覆盖导致"删除后重启复活"（主应用/悬浮窗写前同步）
- 查询记录时间排版优化（小字号单行、贴分界线）

## v0.6.6（2026-08-16）
- 查询记录永久保存（不再随 04:00 清空）
- 角标/今日/累计三处计数统一（角标 = 累计查询次数）

## v0.6.4（2026-08-16）
- 发音机制改为 Web Speech 优先 + 有道在线发音兜底（无系统 TTS 也能发声）

## v0.6.0 之前
- 生词本 / 查询记录 / Review 记忆曲线 / 番茄钟 / 任务目标奖励 / 悬浮窗查词等核心功能逐步成型

> 完整历史版本（v0.1.0 → v0.6.3）为内部迭代，此处从 v0.6.4 开始记录对外版本。

## v0.7.5（2026-08-16）
- 修复导出数据完整性：今日生词/历史生词导出（PDF/Word/PNG）前先异步加载全部 ECDICT 分片，确保每个单词的音标与释义完整带出，不再出现"只有单词名称、缺音标释义"的空缺条目

## v0.7.6（2026-08-16）
- 修复悬浮条异常放大 + 渲染异常：panel max-height + 延迟 reportSize + Java resize 兜底钳制（三层防 IME/内容异常撑大）

## v0.7.7（2026-08-16）
- 修复 v0.7.6 悬浮条布局回退：恢复 v0.7.5 原版 overlay.html（撤销 max-height:min(420px,80vh) 等破坏性改动——vh 在内容撑高的悬浮窗 WebView 中循环导致面板被压坏）
- 悬浮条 bug 改用最小侵入修复：仅 Java resize 兜底钳制高度上限（560dp），CSS/JS 零改动，布局与 v0.7.5 完全一致

## v0.7.8（2026-08-16）
- 修复悬浮条查询结果内容截切（截图 facet 音标被裁）：在 renderDetail/renderHits 后用 requestAnimationFrame 延迟一帧重测尺寸，避开 IME 抖动/刚 innerHTML 后的中间值
- Java resize 加 IME 抖动忽略：focusable 翻转后 600ms 内的偏小 resize 上报视为 IME 抖动误测忽略，避免 params.height 被设小导致内容被 #panel overflow:hidden 截切
- CSS/UI 零改动，仅行为层面修复

## v0.7.9（2026-08-16）
- 修复悬浮条连续快速搜索（如 able→abl→ab→a 连续删除）时偶现的透明浅影/残留阴影：
  - JS reportSize 用 rAF 合并（同一帧多次触发只上报一次），实时搜索机制不变、无延迟
  - Java resize 幂等去重：尺寸与当前一致时跳过 updateViewLayout，避免 Window 合成层反复重建产生残影

## v0.7.10（2026-08-16）
- 修复 v0.7.9 仍存在的「透明浅影/异常大窗口」：v0.7.7 的 560dp 兜底钳制单位错误（`560*d` = 1540px 物理 @ d=2.75 远大于异常窗口，未能拦截）→ 修正为 CSS 600px 物理（`600*d` = 1650px @ d=2.75，正常 panel ~1100px 不误伤，异常 2000+ 被截）
- IME_JITTER 忽略偏小增加「自我修复」例外：若 params.height 已异常大（>maxH，v0.7.7 残留/首帧异常），仍允许偏小 resize 修正 → 恢复正常（避免持久大窗口）
- attachIfNeeded 首次也应用 maxH 钳制

## v0.7.11（2026-08-16）
- 收敛悬浮条高度钳制到设计上限：正常 panel 最大 ~416 CSS px（输入框 60 + 结果区 300 + 内边距 26 + 边框），Java/JS 双端上限统一为 440 CSS px（余量 5.7%）
- 之前 600 CSS px 上限仍比异常窗口（截图量出 ~550）高，异常值被截后仍显"大窗口"；440 后异常被截到接近正常尺寸

## v0.7.12（2026-08-16）
- 修复"搜索词删除为空"时透明浅影/残影（用户精确定位触发点）：
  - 根因：input 事件空状态分支（if !raw）提前 return，漏调 reportSize → params.height 停留在旧结果区高度 → 悬浮条下方残留大片透明区域
  - 修复：空状态分支 innerHTML 清理词条后，用新增 AndroidOverlay.resizeForce() 强制上报收敛高度（绕过 IME 抖动忽略，确保立即生效）
  - Java：resize 重构为 resizeInternal(w,h,force)，新增 resizeForce 桥接
